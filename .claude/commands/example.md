Build and run a sailor example application.

Example: $ARGUMENTS (e.g., "hello", "counter", "dashboard")

Steps:
1. Check if the example exists in `examples/` directory
2. If not, list available examples
3. Build: `zig build example -- <name>`
4. If build fails, analyze and fix
5. Run the example interactively
6. Report any issues

If user says "new <name>", create a new example application skeleton.
