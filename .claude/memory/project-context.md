✅ **Session 442** — FEATURE MODE (2026-09-04, counter=442). CI green (2 success + 1 cancelled of last 3 runs), 0 open issues anywhere. Working tree clean at start. `build.zig.zon` reads 2.99.0, latest tag v2.99.0 (matches, no drift).
  - First caught up milestone bookkeeping: session 441's `configeditor.zig` fix (cf48ce4) had landed but `docs/milestones.md`'s v2.100.0 checklist still showed it `[ ]` — marked `[x]` with the resolution detail before starting new work.
  - Continued the v2.100.0 Widget Doc-Comment Audit Round 3 sweep. Regenerated the unaudited-widget candidate list and found the prior `comm`-based recipe (session 435) under-counted: session 436's "confirmed clean" list used bare names in backticks (e.g. `` `activity_feed` ``, no `.zig` suffix), which the old regex (`` `[a-z_]+\.zig` ``) didn't match, so those files looked unaudited when they weren't. Fixed the regex to `` `[a-z_]+(\.zig)?` `` — this dropped the apparent-unaudited count from 80 to the real 44.
  - Dispatched an Explore agent over those 44 files. Found 8 genuine doc-comment-vs-implementation gaps: `paragraph.zig` (RTL/bidi + word/char wrap never wired, largest surface), `pager.zig` (`wrap` field never consulted, doc's own comment admits it), `metrics_dashboard.zig` (`show_graphs` explicitly discarded with a `// Future:` comment), `terminal.zig` (`AnsiParseState` fully built but never invoked from `addLine()`), `richtext.zig` (doc claims emoji search, only category nav exists), `toggle_switch.zig` (`on_label`/`off_label` stored but never rendered), `timeline.zig` (`TimelineEvent.description` never read), `pipeline.zig` (`PipelineStage.progress` never read). 36 other files confirmed clean.
  - Picked `toggle_switch.zig` as the smallest/cleanest of the 8 (same "smallest first" precedent as hex_editor.zig, session 436). Full TDD cycle: wrote the design into `.claude/scratchpad.md` myself first (render() writes the current state's label — `on_label` if checked, else `off_label` — after a separator space following the switch's own `label` text, truncating at the same `label_space` bound; deliberately left the fixed 6-cell track glyph rendering untouched, out of scope). `test-writer` added 5 RED tests (4 failed for the right reason — space instead of expected letter — 1 passed immediately as a regression guard for unrelated existing behavior). Implemented GREEN directly (single-file change, per CLAUDE.md's "단일 파일 수정" rule). Independently verified: reviewed the full diff (matches the scratchpad spec exactly, 123 insertions), ran `zig build test` twice (exit 0 both times, no fail/error matches). Committed (05a8d08) + pushed.
  - **Release judgment**: 5 unreleased commits since v2.99.0 (hex_editor.zig, filebrowser.zig x2, configeditor.zig, toggle_switch.zig), milestone v2.100.0 still open (7 of the 8 newly-found gaps remain, plus the milestone's own "release" checklist item) — correctly did not force a release, continuing the accumulate-then-bundle precedent from v2.97.0-v2.99.0.
  - **Process insight**: the approximate-list-generation gotcha flagged speculatively in session 435's memory ("treat the unaudited count as a floor, not exact") turned out to be a real, concrete bug this session (44 vs. the stale 80) — worth periodically re-deriving the audited-name list with both backtick forms (bare name and `.zig`-suffixed) rather than trusting a previously-generated `/tmp` file across sessions, since milestone-checklist authors haven't been consistent about which form they use.
  - **Memory maintenance**: compressed sessions 394-419's 14 detailed entries into one summary paragraph (file was at 197 lines, over the 200-line soft budget) — preserved the load-bearing lessons (verify-wiring-independently, checked-box-isn't-a-release recurring a 3rd time, TDD-agent-boundary held under multi-re-dispatch pressure, Infinity-vs-NaN empirical-verification requirement, and that this span is where the doc-comment-audit method was first discovered), dropped only the individually-resolved bug specifics.
  - **Current state**: `build.zig.zon` unchanged (2.99.0), latest tag v2.99.0. 0 open issues anywhere. 1 active milestone (v2.100.0 — 3 of the original+new gaps now fixed: hex_editor.zig, filebrowser.zig, configeditor.zig, toggle_switch.zig; 7 remain from this session's batch — see `docs/milestones.md` for the smallest-first ordering, `pipeline.zig`/`timeline.zig`/`terminal.zig`/`pager.zig` are medium scope, `metrics_dashboard.zig`/`richtext.zig` need a scope decision, `paragraph.zig` is largest and flagged for an `architect` pass). 5 unreleased commits on main. Next session should continue the v2.100.0 sweep — pick the next-smallest of the 7 remaining gaps (likely `timeline.zig` or `pipeline.zig`) — or reconsider bundling a release given 5 commits have now accumulated.

✅ **Session 441** — FEATURE MODE (2026-09-04, counter=441). CI green (3/3 recent runs success or cancelled-only), 0 open issues anywhere. Working tree was NOT clean at start with two distinct problems, handled separately:
  1. **Build-breaking artifact, discarded (not committed)**: 5 widget files (`autocomplete.zig`, `calendar.zig`, `canvas.zig`, `checkbox.zig`, `chunkedbuffer.zig`) each had a stray `comptime { @compileError("PROBE_MARKER_<name>"); }` line prepended — confirmed via `zig build test` (5 compile failures) that this broke the build outright. Matching `/tmp/triggered_probes.txt`/`/tmp/batch_a[a-d]_result.txt` files (from an apparent prior ad-hoc "which widgets are actually reachable by `zig build test`" sweep, never committed to the repo) show these 5 were already confirmed "triggered"/reachable — so the probes were leftover debug scaffolding, not WIP. `git checkout --` reverted all 5; build was clean afterward (0 failures).
  2. **Legitimate session-439/440 WIP, completed (GREEN phase)**: `configeditor.zig` had ~440 lines of RED tests for `ConfigEditor` scalar-value editing (`startEdit`/`editText`/`insertChar`/`deleteChar`/`confirmEdit`/`cancelEdit`/`selectedNode`), referencing fields/methods that didn't exist yet (`.claude/logs/agent-activity.jsonl` confirmed a `test-writer` dispatch on 2026-09-03/04 for exactly this). Implemented the GREEN half directly (single-file change, per CLAUDE.md's rule): added `edit_buffer: []u8`/`edit_len: usize`/`is_editing: bool` fields plus the 7 methods above — `startEdit` no-ops on a zero-length `edit_buffer`, null/out-of-range/non-scalar (`object`/`array`) selection; `selectedNode()` walks the same `flattenNodesStack` the renderer uses (selection indices are flat-tree, not top-level-array, indices — confirmed by the "nested structure" RED test); scalar formatting reuses `{d}` for numbers (matches `renderValue`'s convention) but does NOT add quotes around strings (edit text is raw/unquoted, unlike the display-only quoted rendering).
  - **Second, independent finding while investigating the same file**: `ConfigEditor` was never actually reached by any `zig build test` root — its `pub const` re-export in `tui.zig` is never *used* anywhere, so its own ~1400 lines of inline `test` blocks (both the pre-existing ones and the new RED tests) never compiled or ran, confirmed by appending a throwaway `@compileError` probe to the file and observing `zig build test` exit 0 with zero mention of it. Cross-referenced against `/tmp/orphaned_widgets.txt`/`/tmp/unaudited.txt` (stale files from an earlier, apparently abandoned reachability audit) — both independently list `configeditor.zig` as orphaned, alongside ~30 other now-mostly-already-fixed widgets (most of that list — autocomplete/calendar/canvas/checkbox/chunkedbuffer/filebrowser/hex_editor — are confirmed fixed via existing `tests/*_test.zig` files or recent session commits; the remainder is a stale, unverified list a future stabilization session could re-check, not to be trusted as current). **Fix**: created `tests/configeditor_test.zig` (references `sailor.tui.widgets.ConfigEditor`/`ConfigNode`, forcing full analysis, plus 2 new black-box integration tests) and registered it in `build.zig` (3 additions: `addTest`, `addImport`, `test_step.dependOn` — following the exact `filebrowser_tests` pattern). Re-verified with the same probe technique: the probe now fires (proving reachability), and the real file passes clean.
  - Verified independently before committing: `zig build test` (344/344 steps, 12475/12531 tests passed, 56 skipped, exit 0), plain `zig build` (exit 0). Committed (`cf48ce4`, 4 files: configeditor.zig, build.zig, tests/configeditor_test.zig, agent-activity.jsonl log entry) + pushed. Did NOT commit the reverted probe-marker files (correctly clean, no diff after `git checkout --`).
  - **Process insight — a new failure mode for "trust but verify"**: this session establishes that `zig build test` exit-0/passing is *not sufficient* evidence that a given widget file's tests actually ran — a file can be fully orphaned from the analysis graph (silently 0 tests executed) while the overall build reports success. Future sessions encountering a widget with substantial inline tests but suspiciously absent from any `tests/*_test.zig` cross-reference should verify reachability directly (append a throwaway `@compileError` probe, confirm it fires, then remove it) rather than assuming `zig build test`'s green exit code covers it.
  - **Release judgment**: 1 unreleased commit since v2.99.0 (this session's cf48ce4) — v2.100.0 milestone still open (per session 438's note, "~100 widget files still unaudited by this round" remains true; the orphaned-widget-reachability finding is a related-but-distinct gap, not itself a milestone checklist item). Correctly did not force a release.
  - **Current state**: `build.zig.zon` unchanged this session (check next session for exact value/latest tag). 0 open issues anywhere. 1 active milestone (v2.100.0 — `configeditor.zig`'s scalar-editing gap now closed both functionally and in terms of test reachability; remaining scope is the wider unaudited-widget sweep). 1 unreleased commit on main. Next session should either continue the v2.100.0 Explore sweep over remaining unaudited widgets, or — given this session's reachability finding — consider a dedicated pass cross-referencing `/tmp/orphaned_widgets.txt`'s ~30-widget list against current `tests/*_test.zig` coverage to find any other genuinely-still-orphaned files (most entries are likely already stale/fixed, per this session's spot-check, but not verified exhaustively).

✅ **Session 438** — FEATURE MODE (2026-09-03, counter=438). CI green (last 3 runs success/queued), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree was NOT clean at start — found session 437's leftover uncommitted work: `test-writer` had written 5 RED tests for `filebrowser.zig`'s preview-pane split-render (preview-disabled regression guard, too-narrow width<20 skip, file-content preview, directory-info preview, empty-entries no-crash) plus a debug-only `build.zig` change (`.filters = &.{"FileBrowser render"}` on the test step, scoping `zig build test` to just those 5 tests during RED-phase iteration — never meant to be committed).
  - Completed the GREEN half directly (single-file change, matches CLAUDE.md's "단일 파일 수정 → test-writer 호출 후 직접 구현" rule — test-writer already ran RED last session). Implemented the split-pane design the milestone note called for: `render()` now computes `show_preview = enable_preview and inner_area.width >= 20`, splits `inner_area` into a `list_area` (left) and a `preview_area` (right of a `│` divider at `(width-1)/2`), and renders `getFilePreview()`/`getDirectoryInfo()` output for the currently selected entry into the preview pane via a scratch `ArenaAllocator` (freed every render call — avoids leaking on repeated draws). Kept the divider math simple fixed-ratio rather than reusing `splitpane.zig`'s constraint solver, since there's only ever one always-50/50 split here — matches the "no over-engineering" rule.
  - Removed the debug-only `.filters` line from `build.zig` before committing (would have silently narrowed every future `zig build test` run in CI/local to only 1 test name — a testing-infrastructure landmine if left in). Ran the full `zig build test` (no filters) after the fix: exit 0, no fail/error matches. Reviewed the diff myself before committing. Committed (372a439, filebrowser.zig only) + pushed, then a second small commit (feff3a9) updating `docs/milestones.md`'s v2.100.0 checklist to mark the filebrowser.zig gap `[x]`.
  - **Process insight**: this is the 2nd session in a row (after 436→437's carryover) where a RED-phase-only cycle left uncommitted state for the next session to discover and finish — worth noting the multi-session TDD handoff pattern is working as intended (scratchpad + memory + `.claude/logs/agent-activity.jsonl` together gave enough context to resume cold), but a stray build-config debug artifact (`.filters`) almost rode along uncommitted-then-forgotten. Future sessions should scan `git diff build.zig`/`build.zig.zon` specifically for scaffolding leftovers (test filters, temporary target restrictions) before committing, not just the feature file.
  - **Release judgment**: 2 unreleased commits since v2.99.0 (d239daa hex_editor.zig + this session's filebrowser.zig), milestone v2.100.0 still not complete (`configeditor.zig` design decision + remaining unaudited-file sweep still open) — correctly did not force a release, continuing the accumulate-then-bundle precedent.
  - **Current state**: `build.zig.zon` reads 2.99.0, latest tag v2.99.0 (unchanged, no release this session). 0 open issues anywhere. 1 active milestone (v2.100.0 — 2/3 confirmed gaps now fixed: hex_editor.zig, filebrowser.zig; `configeditor.zig` remains, still needs the editing-scope design decision flagged in session 436; ~100 widget files still unaudited by this round). 2 unreleased commits on main. Next session should tackle `configeditor.zig`'s scope decision (read the file fresh, decide whether "editing" means value mutation, expand/collapse toggling, or both, before dispatching test-writer) or continue the Explore sweep over the remaining unaudited widget batch — either is a reasonable next pick.

✅ **Session 436** — FEATURE MODE (2026-09-03, counter=436). CI green (last 3 runs success), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start, `build.zig.zon` (2.99.0) matched latest tag (v2.99.0). Started the v2.100.0 Widget Doc-Comment Audit Round 3 milestone: dispatched an Explore agent over the first 39-file candidate batch from the milestone checklist. Found 3 genuine gaps — `configeditor.zig` (claims "editing" but is 100% read-only, no mutation/key-handling anywhere — deferred, needs a scope design decision), `filebrowser.zig` (`enable_preview`/`withPreview()`/`getFilePreview()` fully implemented but never wired into `render()`, no preview pane ever drawn — deferred, needs a split-pane render design), `hex_editor.zig` (`modified_style` field + `withModifiedStyle()` builder existed as dead plumbing, no way to mark or render "modified" bytes — fixed this session, smallest/cleanest of the 3). 36 other files in the batch confirmed clean.
  - Fixed `hex_editor.zig`: designed the fix myself (scratchpad-first, same practice as sessions 423-435) — added `modified: []const bool` borrowed parallel slice (same convention as the existing `data` field) + `withModified()` builder; `render()`'s hex-byte loop now applies `modified_style` with `cursor > modified > base` precedence, deliberately scoped to the hex column only (matches the ASCII column's existing behavior of not applying `cursor_style` either — not extended as part of this fix, kept the change surgical).
  - Full TDD cycle: `test-writer` wrote 6 RED tests (confirmed compile-error RED — missing `withModified` — before GREEN). Implemented GREEN directly (single-file change, per CLAUDE.md's "단일 파일 수정 → test-writer 호출 후 직접 구현" rule). First `zig build test` run after GREEN surfaced 4 runtime assertion failures — traced to a **test bug, not an implementation bug**: the tests assumed byte N's hex nibbles land at columns `3*N`/`3*N+1`, but the default `group_size=1` means the "extra space at group boundaries" block fires after every byte (in addition to the regular byte-separator space), so real columns are `4*N`/`4*N+1`. Per the TDD-agent-boundary rule, sent back to `test-writer` to fix its own column math rather than patching directly — re-verified independently after (full `zig build test`, then a second confirming run since the first run also showed one unrelated flaky failure, `validation.test.async validator - non-blocking`, that was gone on rerun and untouched by this diff). Reviewed the full diff myself before committing (251 insertions, hex_editor.zig only — field, builder, precedence change, 6 tests). Committed (d239daa) + pushed.
  - **Release judgment**: single `feat:` commit, not a milestone completion — correctly did not force a release. 1 unreleased commit on main since v2.99.0, continuing the accumulate-then-bundle precedent.
  - **Process insight**: this is now at least the 3rd time a test-writer RED-phase test encoded an assumption about exact render() column math that turned out wrong once GREEN was implemented and runtime-verified (distinct from a compile-error RED, which is easy to get right) — worth flagging that any test asserting *exact pixel/cell coordinates* in a widget with configurable spacing (group_size, padding, etc.) should be double-checked against the *default* value of every such config field, not just the widget's headline behavior, since defaults can introduce non-obvious extra spacing.
  - **Current state**: `build.zig.zon` reads 2.99.0, latest tag v2.99.0 (unchanged, no release this session). 0 open issues anywhere. 1 active milestone (v2.100.0, in progress — 1/3 confirmed gaps fixed this session: hex_editor.zig; 2 remain, both need a design decision before their own TDD cycle: configeditor.zig's editing-scope question, filebrowser.zig's split-pane render design; ~100 widget files still unaudited by this round). 1 unreleased commit on main. Next session should pick `filebrowser.zig`'s preview-pane wiring (has a ready-made `getFilePreview()` helper to reuse, only needs a render-path split design — smaller design surface than configeditor.zig's open-ended "what does editing mean" question) or continue the Explore sweep over the remaining unaudited widget batch.

✅ **Session 435** — STABILIZATION (2026-09-03, counter=435). CI green (last 5 runs success/cancelled-only), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. `zig build test` 0 failures. Ran the full stabilization protocol including the 6-target cross-compile sweep (sequential, `pgrep -f "zig build"` confirmed clear first — all 6 targets OK, this time the per-target `for` loop worked fine as separate iterations within one Bash call, unlike session 425's reported shell-splitting quirk).
  - **Release judgment**: v2.99.0 milestone (Widget Doc-Comment Audit Round 2) was already 100% checklist-complete as of session 434, only the release action itself was pending — all 3 release gates met (tests, 6-target cross-compile, 0 bug issues). Executed a MINOR release autonomously: bumped `build.zig.zon` 2.98.0→2.99.0, updated `docs/milestones.md` (moved v2.99.0 to Completed Milestones table, marked its own entry Complete), committed (6a97e84) + pushed, tagged v2.99.0 (plain `-m` string, not heredoc — per session 428's documented `git tag` heredoc gotcha), GitHub release created, migration issues filed (zr#152, zoltraak#118, silica#133), Discord notified.
  - **Milestone replenishment**: 0 active milestones after the release triggered the "≤2 active → replenish" rule. Checked `gh issue list --label feature-request` across all 4 repos first (empty) — fell to source #4 (기술 부채): grepped `src/` for TODO/FIXME (zero hits, nothing there), then diffed the full `src/tui/widgets/*.zig` file list (140 files) against every `` `foo.zig` `` backtick-reference in `docs/milestones.md` (66 matched — an approximation, not a perfect audit-completion tracker) to find 83 files never referenced by name in any audit round. Established v2.100.0 "Widget Doc-Comment Audit Round 3" with the first ~40 unaudited filenames listed directly in the checklist so next session can start immediately without re-deriving the list. Used v2.100.0 (not v3.0.0) — semver minor-field increment, no MAJOR bump without explicit user instruction, per CLAUDE.md's version-safety rule.
  - **Process insight**: the `` `foo.zig` ``-backtick-grep method for tracking audit coverage is approximate (catches only files explicitly named in a milestone checklist, not files that were checked and found clean without an explicit backtick mention elsewhere) — good enough for scoping a new round's checklist, but future sessions should treat the "83 unaudited" figure as a floor, not an exact count.
  - **Current state**: `build.zig.zon` reads 2.99.0, latest tag v2.99.0 (matches — released this session). 0 open issues anywhere. 1 active milestone (v2.100.0 Widget Doc-Comment Audit Round 3, not yet started — candidate list already in `docs/milestones.md`). 0 unreleased commits on main. Next session (feature mode) should start v2.100.0 by dispatching an Explore agent over the listed candidate widgets for the same doc-comment-vs-implementation mismatch pattern.

✅ **Session 432** — FEATURE MODE (2026-09-02, counter=432). CI check skipped (no `gh run list` needed — 0 open issues confirmed via `gh issue list`, working tree was NOT clean at start). Found session 431's leftover uncommitted work: `test-writer` had written 8 RED tests for `MultiCursorEditor.undoAll`/`redoAll` in `src/tui/widgets/multicursor.zig` (confirmed via `.claude/logs/agent-activity.jsonl`'s last entry and grep — `undoAll`/`redoAll` referenced only in tests, no implementation existed), plus an already-complete-looking `editor.zig` undo()/redo() hardening fix (errdefer-based cleanup around the Zig-0.15 optional-returning `ArrayList.pop()`) and a `context_menu.zig` triage note in `docs/milestones.md` — both looked finished, just never committed.
  - Implemented the GREEN half directly (single-file change, matches CLAUDE.md's "단일 파일 수정 → test-writer 호출 후 직접 구현" rule — test-writer had already run the RED phase last session, no need to re-dispatch): `MultiCursorEditor` gained `undo_batches`/`redo_batches: ArrayList(EditBatch)` fields, where `EditBatch` is a **full-buffer + full-cursor-set snapshot** (before and after), not a per-character edit list like `base: Editor`'s own undo stack. Chose the full-snapshot approach deliberately over replaying individual per-cursor `Editor.Edit`-style character edits, because two cursors can land on the same line and the existing `insertCharAll`/`deleteCharAll` already has same-line index-shift logic that's fragile to reorder — a whole-buffer snapshot sidesteps that correctness risk entirely at the cost of a full line-array dupe per batch op (acceptable for a niche multi-cursor batch-edit feature, not a hot path). `insertCharAll`/`deleteCharAll` now snapshot before the edit, run their existing logic unchanged, then snapshot after and push the batch (clearing `redo_batches`, mirroring `Editor`'s own clear-redo-on-new-edit convention). `undoAll`/`redoAll` restore the relevant snapshot in one step and move the batch to the other stack.
  - Verified independently before committing: `zig build test` from the existing cache (exit 0, no leaks — `testing.allocator` would have failed the run on any snapshot double-free/leak), then a second full `zig build test` run to confirm, then plain `zig build` (library-only). Reviewed the diff myself. Committed (7cb2d54) + pushed, bundling the session-431 leftovers (editor.zig fix, context_menu.zig triage note) in the same commit since they were already complete and just needed a commit.
  - Updated `docs/milestones.md`'s v2.99.0 checklist: split the `multicursor.zig` line into two — undo/redo now `[x]` with implementation detail, "Ctrl+D for next occurrence" still `[ ]` (not started, second half of the original session-428 2-cycle split).
  - **Release judgment**: v2.99.0 milestone still not complete (next-occurrence item open) — correctly did not force a release. This adds 1 more unreleased commit on top of whatever was unreleased after session 430's notification.zig fix.
  - **Current state**: `build.zig.zon` unchanged this session (check next session for exact unreleased-commit count since last release tag). 0 open issues anywhere. 1 active milestone (v2.99.0 — 2/3 checklist items now done: notification.zig, multicursor.zig undo/redo; context_menu.zig triaged as not-a-bug; only "Ctrl+D for next occurrence" cursor addition remains, plus the "confirmed clean" file list and final release step). Next session should implement Ctrl+D next-occurrence (search-forward-from-cursor for the next match of the current selection/word, add a cursor there — same family of feature as the undo/redo work, may reuse `MultiCursorEditor.addCursor` directly) to close the milestone, or check whether accumulated unreleased commits justify bundling a release first.

✅ **Session 430** — STABILIZATION (2026-09-01, counter=430). CI green (last 5 runs success/cancelled-only, no reds), 0 open issues anywhere. Working tree was NOT clean at start — found uncommitted `src/tui/tui.zig` + `src/tui/widgets/notification.zig` changes: a complete, working `withTimeout()`/`dismiss()`/`tick()` auto-dismiss feature (matches the v2.99.0 milestone's notification.zig checklist item exactly) that a prior session (428, going by the memory entry's description) had implemented but apparently never committed — likely session 429 ran and didn't leave a memory entry, or 428's own commit step was interrupted. Verified it built and passed (`zig build test`, no failures) before committing (8f5f014) + pushing, then marked the milestone checklist item `[x]` in `docs/milestones.md`.
  - Ran the full stabilization protocol: 6-target cross-compile, sequential per policy (`pgrep -f "zig build"` confirmed clear first) — x86_64/aarch64-linux-gnu, x86_64/aarch64-macos, x86_64-windows-gnu/msvc all clean. Test-quality audit: dispatched `test-writer`, steered away from exhausted wells (NaN/Infinity, weak disjunctions, carousel.zig, notification.zig, and the actively-mid-milestone tooltip.zig/splitpane.zig). Found `accordion.zig` — 17 public functions, only a `refAllDecls` compile-check, zero behavioral tests (confirmed via `git log --oneline -15 -- src/tui/widgets/accordion.zig`, untouched since its original v2.30.0 implementation commit — no overlap with active work). Agent added 55 tests (init defaults, toggle/expand/collapse incl. single_expand mode and out-of-bounds no-ops, cursor wrap navigation, isExpanded queries, builder immutability, render edge cases).
  - Independently verified before committing (per standing "don't trust agent self-reports" practice): reviewed the full diff myself. Caught one weak-disjunction assertion the agent introduced (`cell.style.bold or cell.style.reverse` inside a silently-skippable `if (buf.getConst(...)) |cell|`) — exactly the previously-exhausted pattern the brief said to avoid, reintroduced despite the explicit steer. Fixed it myself: `cursor_style`'s default sets *both* `bold` and `reverse`, so tightened to a non-optional `.?` unwrap plus two separate `expect()` calls, both required. Reran `zig build test` from a clean `.zig-cache` myself after the fix (0 failures). Committed (b06554d) + pushed.
  - **Release judgment**: 4 unreleased commits since v2.98.0 (2 feat/test this session + 2 carried from before). Milestone v2.99.0 NOT complete — 2 of 3 checklist items remain open (`multicursor.zig` undo/redo + Ctrl+D next-occurrence, bigger design surface flagged as possibly needing an `architect` pass; `context_menu.zig` submenu navigation, still needs a read-the-file-first triage before assuming it's a real gap). Correctly did not force a release — continuing the accumulate-then-bundle precedent from v2.97.0-v2.98.0.
  - **Process insight**: agent self-reports on test quality still need verification even from `test-writer` specifically briefed with the exact anti-patterns to avoid — the brief listed "weak-disjunction assertions... long since swept" as a well already exhausted, and the agent's own new test reintroduced exactly that shape in a part of the file the brief didn't directly examine (render-time style assertions). Read-the-full-diff-yourself remains non-negotiable even for narrowly-scoped, well-briefed agent tasks.
  - **Current state**: `build.zig.zon` reads 2.98.0, latest tag v2.98.0 (no drift — correctly not bumped, no release this session). 0 open issues anywhere. 1 active milestone (v2.99.0 — 1/3 items now done: notification.zig; multicursor.zig and context_menu.zig remain). 4 unreleased commits on main. Next session (feature mode) should pick `multicursor.zig`'s undo/redo — likely warrants an `architect` pass first per the milestone note's own suggestion (bigger design surface than the tooltip/splitpane/notification precedents) — or triage `context_menu.zig` first since it's cheaper (single read-through may resolve it as "not a bug, close the checklist item" rather than requiring implementation).

✅ **Session 428** — FEATURE MODE (2026-09-01, counter=428). CI green (last 3 runs success), 0 open issues anywhere. Working tree clean at start. Closed the v2.98.0 Widget Doc-Comment Audit milestone's last remaining item: tooltip.zig's **"optional fade-in animation"**. No blend/alpha/lerp precedent existed anywhere in the codebase (grepped `blend|lerp|alpha` across `src/tui` — zero real hits) — genuinely new design, resolved by the orchestrator into `.claude/scratchpad.md` before dispatch, same practice as sessions 423-427. Key design call: blending is scoped to `.rgb` colors only (lerped from black toward the target as alpha ramps 0→1) since named/indexed ANSI colors have no interpolatable components — those render at full color immediately, a documented scope boundary rather than a gap. Added `fade_in_ticks`/`fade_ticks_elapsed`/`withFadeIn()`/`currentAlpha()`, wired into `tick()` (fade progresses independently of, but on the same tick as, the existing timeout countdown) and `render()` (content style, block border style via `Block.withBorderStyle`, and arrow style all blend).
  - Full TDD cycle, 2 test-writer bugs caught and correctly sent back rather than fixed directly (TDD-agent-boundary rule held twice): (1) two tests asserted a default NAMED-color `border_style` (`.bright_yellow`) would blend to `.rgb` after fading — contradicts the rgb-only design, fixed by constructing an explicit rgb `border_style` and asserting exact hand-computed blended values instead of a weak "is it .rgb or not" check; (2) an off-by-one in a pending-phase test — `withShowDelay(3)` completes the delay exactly on the 3rd `tick()` call (established behavior, not new), so a test ticking 3 times before asserting "still pending" was wrong; fixed to tick twice. Independently verified before committing: reviewed the full source diff myself (matches the scratchpad spec exactly, 463 insertions across fields/builder/tick/render/2 new module-private helpers), reran `zig build test` from the existing `.zig-cache` twice (grepped for fail/error, none found; also ran the plain command and read the summary — exit 0 both times). Committed (83c0a33) + pushed.
  - **Release**: milestone now fully complete (4/4 tooltip.zig gaps + splitpane.zig drag-handle, 6 unreleased commits since v2.97.0) — met all 3 release gates (tests, 6-target cross-compile via CI, 0 open bug issues) and executed a MINOR release autonomously per protocol. Bumped `build.zig.zon` to 2.98.0, tagged, GitHub release created, migration issues filed (zr#151, zoltraak#117, silica#132), Discord notified. **Note**: `git tag -a` with a heredoc `-m` body failed silently (exit 0, but `git tag -l` showed no tag created, and the follow-up push errored "does not match any") — worked fine with a plain single-line `-m` string instead. Worth a heads-up for future sessions: avoid heredoc `-m` bodies specifically for `git tag`, even though the same pattern works fine for `git commit -m`.
  - **Milestone replenishment**: 0 active milestones after the release triggered the "≤2 active → replenish" rule. Checked `gh issue list --label feature-request` across all 4 repos first (empty, only the migration issues this session just filed) — fell to source #4 (기술 부채), dispatched an Explore agent over a fresh batch of 18 interactive/stateful widget files (deliberately avoiding already-exhausted chart widgets, audited for a different bug class in the Infinity Safety Audit). Found 2 strong candidates (`notification.zig`'s claimed-but-missing auto-dismiss/manual-close — direct precedent already exists in `toast_manager.zig`'s tick/dismiss pattern; `multicursor.zig`'s claimed-but-missing undo/redo and Ctrl+D next-occurrence, a bigger design surface that may warrant an `architect` pass) and 1 weaker one (`context_menu.zig`'s submenu navigation — possibly intentional API design, needs a closer read before assuming it's a bug). 16 files confirmed clean. Established v2.99.0 milestone in `docs/milestones.md` with this lead, so next session can start implementing immediately rather than re-running discovery.
  - **Process insight**: the "rgb-only blending, named colors unblendable" design boundary set up front (rather than trying to force a universal blend for all color kinds, which isn't well-defined for ANSI named colors) kept the implementation small and the test-writer handoff mostly clean — the only test bugs were misapplying that boundary (using a named color where the test needed rgb to demonstrate blending), not a flaw in the boundary itself. Consistent with sessions 423-427's lesson that resolving the API/design shape completely before dispatch (not leaving any judgment call to test-writer) minimizes re-dispatches.
  - **Current state**: `build.zig.zon` reads 2.98.0, latest tag v2.98.0 (matches, released this session). 0 open issues anywhere. 1 active milestone (v2.99.0 Widget Doc-Comment Audit Round 2 — not yet started, 3 candidates queued: notification.zig auto-dismiss, multicursor.zig undo/redo, context_menu.zig submenu nav needs triage first). 0 unreleased commits on main. Next session should start v2.99.0 with `notification.zig`'s auto-dismiss (strongest candidate, has a ready-made precedent in `toast_manager.zig` to reuse, same as tooltip.zig's timeout fix reused it).

✅ **Session 427** — FEATURE MODE (2026-09-01, counter=427). CI green (last 3 runs success), 0 open issues anywhere. Working tree clean at start. Continued the v2.98.0 Widget Doc-Comment Audit milestone — picked the first of the 2 remaining tooltip.zig doc-comment promises flagged since session 423: **`Trigger` enum (hover/focus/manual) wiring**. Designed the API myself before dispatch (written into `.claude/scratchpad.md`), reusing the same "pure, event-loop-agnostic method a caller wires to their own event source" pattern established for splitpane.zig's `isOnDivider`/`resizeAt` (session 426) — sailor owns no mouse/focus event loop, so wiring means exposing state-transition methods, not a real handler.
  - Full TDD cycle: `test-writer` added 24 RED tests to `src/tui/widgets/tooltip.zig` (confirmed compile-error RED — "no field named 'trigger'" etc. — before implementing). Implemented directly (single-file change, per CLAUDE.md's rule): `trigger: Trigger = .manual` field, `withTrigger()` builder, `notifyHover(hovering, target_area)` and `notifyFocus(focused, target_area)` — each a no-op unless `self.trigger` matches the respective enum value, and each delegates to the existing `show()`/`hide()` internally (so `show_delay_ticks` pending-phase logic is reused, not duplicated). Default `.manual` preserves all existing direct-call behavior for every existing caller/test unchanged.
  - Independently verified before committing: reran `zig build test` from the existing `.zig-cache` (grepped output for `fail|error`, none found, exit 0), reviewed the full diff myself (317 insertions, tooltip.zig only: 2 fields/builders + 2 new methods + 24 tests). Committed (1db0cf4) + pushed.
  - **Release judgment**: single `feat:` commit, not a milestone completion — correctly did not force a release. Now 5 unreleased commits on main since v2.97.0, still following the accumulate-then-bundle precedent.
  - **Current state**: `build.zig.zon` reads 2.97.0, latest tag v2.97.0 (matches). 0 open issues. 1 active milestone (v2.98.0 — 4 of the originally-identified gaps now fixed: tooltip.zig timeout, delay-before-showing, Trigger wiring, plus splitpane.zig drag-handle; only 1 remains, tooltip.zig's "optional fade-in animation," which needs an actual design decision — a frame/alpha or fade_ticks_remaining concept with no existing precedent widget in the codebase for blending styles by alpha). 5 unreleased commits on main. Next session should either design+implement the fade-in animation (closing v2.98.0 entirely, likely triggering a minor release bundling all 5 unreleased commits), or run a fresh Explore pass over more of the 145+ widget files not yet swept for the same pattern.

✅ **Session 426** — FEATURE MODE (2026-08-31, counter=426). CI green (last 3 runs on main all success), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Continued the v2.98.0 Widget Doc-Comment Audit milestone — picked the untriaged `splitpane.zig` "drag handle for interactive mouse-based resizing" doc-comment claim flagged since session 423 (deferred each session since — 425 explicitly punted it to "next session"). Confirmed via grep (`MouseEvent|handleMouse|onDrag|dragStart|dragTo` across `src/tui/widgets/`) that **no widget in the codebase handles mouse/drag events at all** — same "library owns no event loop" shape as tooltip.zig's Trigger enum gap, so the fix pattern was: expose pure, event-loop-agnostic calculation methods a caller wires to their own mouse events, not a real event loop.
  - Full TDD cycle: `test-writer` added 23 RED tests to `src/tui/widgets/splitpane.zig` for two new methods designed by the orchestrator before dispatch (same "resolve the API shape myself, then hand a concrete spec to test-writer" practice sessions 423-424 established) — `isOnDivider(area, x, y) bool` (hit-test for mouse-down on the divider cell; false when `show_divider` is off or position is outside `area`) and `resizeAt(area, x, y) SplitPane` (returns a new value with `split_ratio` recomputed from the pointer position via the same `usable_space` calc `calculatePanes` already uses, clamped to 0.0/1.0 at either edge). Implemented both directly (single-file change, per CLAUDE.md's "단일 파일 수정 → test-writer 호출 후 직접 구현" rule — no separate zig-developer dispatch needed). `resizeAt` deliberately does NOT pre-apply min/max size constraints itself — those still apply downstream the next time `calculatePanes` runs on the result, same as any other `split_ratio` change, so no duplicate constraint logic was introduced.
  - Independently verified before committing: reran `zig build test` from the existing `.zig-cache` (0 failures, 0 errors — grepped output for `fail|error`, none found), reviewed the diff myself (272 insertions, one file: 2 new methods + updated doc comment + the 23 tests already staged by test-writer). Committed (742711f) + pushed.
  - **Release judgment**: single `feat:` commit, not a milestone completion — correctly did not force a release. This is now 4 unreleased commits on main since v2.97.0 (2 tooltip feat commits + 1 carousel test commit + this session's splitpane feat commit) — still following the accumulate-then-bundle precedent from v2.97.0, no threshold explicitly crossed yet this session.
  - **Current state**: `build.zig.zon` reads 2.97.0, latest tag v2.97.0 (matches — no drift). 0 open issues. 1 active milestone (v2.98.0, in progress — 3 of the originally-identified gaps now fixed: tooltip.zig timeout + delay-before-showing + splitpane.zig drag-handle; 2 remain, both tooltip.zig: Trigger-enum hover/focus wiring needs an external notify-hover/notify-focus API design, fade-in animation needs a new frame/alpha concept with no existing precedent widget in the codebase). 4 unreleased commits on main. Next session should pick one of the 2 remaining tooltip.zig promises, or run a fresh Explore pass over more of the 145+ widget files not yet swept for the same doc-comment-vs-implementation gap pattern (the splitpane/tooltip leads are now down to 2 known items).

✅ **Session 425** — STABILIZATION (2026-08-31, counter=425). CI green (last 3 runs on main all success, includes the 6-target cross-compile matrix), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. `zig build test` (clean `.zig-cache`) 0 failures. Ran the full stabilization protocol:
  1. **6-target cross-compile, verified locally too** (not just trusting CI green): x86_64-linux-gnu, aarch64-linux-gnu, x86_64-macos, aarch64-macos, x86_64-windows-gnu, x86_64-windows-msvc — all built clean, run sequentially per policy (`pgrep -f "zig build"` confirmed no concurrent heavy process first). **Tooling note**: an initial attempt to loop all 6 targets in one bash `for t in $targets; do ... done` background command silently misbehaved — the unquoted `$targets` var did NOT word-split as expected inside this session's background-command execution path, so the whole 6-target string got passed as a single `-Dtarget=` value and errored (`UnknownApplicationBinaryInterface`) on the very first "iteration". Not a Zig or policy issue — an artifact of how this harness executes multi-line background bash scripts. Fix: ran each target as its own separate `Bash` tool call instead of a shell for-loop. Worth remembering for future stabilization sessions doing the same 6-target sweep.
  2. **Test-quality audit** (mandatory stabilization step): dispatched `test-writer`, explicitly steered away from the exhausted `@intFromFloat`-NaN/Infinity well (34/34 widgets fully audited, sessions 410/412/414) and the weak-disjunction-assertion well (long since swept), and away from `tooltip.zig` (mid-milestone, v2.98.0, don't touch). Found `src/tui/widgets/carousel.zig` — 23 public functions, **zero test coverage anywhere** (only a `refAllDecls` compile-check, no behavioral assertions at all). Added 33 tests: next/prev loop-vs-clamp semantics (including empty-carousel no-op), goTo bounds rejection, isFirst/isLast/count edge cases (including the `items_count=0` → both true convention), indicatorHeight toggle, contentArea inset math with/without block+padding+indicators (hand-derived exact pixel math, not copied from source), and builder-copy immutability (single setters + a chained `.withCurrent().withLoop()` case proving the original stays untouched at each step). Independently verified before committing: reviewed the full diff (251 insertions, one file, test-only — zero source changes), reran `zig build test` from a clean `.zig-cache` myself (exit 0, 0 failures). No source bug found — pure coverage gap, exactly as expected for a widget with 0% prior coverage. Committed (e330000) + pushed.
  - **Release judgment**: `zig build test` clean + all 6 cross-compile targets green + 0 open bug issues (all 3 release-gate conditions met), but per protocol a release still requires either a `fix:`-only backlog (patch) or a completed milestone (minor) — neither applies: main currently carries 2 unreleased `feat:` commits (session 423 tooltip timeout, session 424 tooltip delay-before-showing) plus this session's 1 `test:` commit, and the active v2.98.0 milestone is explicitly NOT complete (2 of its checklist items still open — Trigger enum wiring and fade-in animation). Correctly did not force a release.
  - **Process insight**: this is now the first stabilization-session test-quality audit in several cycles to land on a "zero coverage on an entire widget" gap rather than a "weak assertion in an existing test" gap — worth treating "grep tests/ and src/tui/widgets/ for a widget with no matching *_test.zig block and no inline `test` beyond refAllDecls" as a standing, cheap first move for future audits, since the previously-exhausted wells (NaN/Infinity, weak disjunctions) are unlikely to yield anything new.
  - **Current state**: `build.zig.zon` reads 2.97.0, latest tag v2.97.0 (matches). 0 open issues. 1 active milestone (v2.98.0, in progress — 2/4 tooltip.zig doc-comment gaps fixed, 2 remain: Trigger-enum hover/focus wiring needs an external notify API design, fade-in animation needs a new frame/alpha concept with no existing precedent). 3 unreleased commits on main since v2.97.0 (2 feat + 1 test). Next session (feature mode) should continue the v2.98.0 milestone — pick one of the 2 remaining tooltip.zig promises, triage `splitpane.zig`'s still-untriaged drag-handle claim, or run a fresh Explore pass over more of the 145+ widget files not yet swept for the same doc-comment-vs-implementation gap pattern.

✅ **Session 424** — FEATURE MODE (2026-08-31, counter=424). CI green (last 3 runs success), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Continued the v2.98.0 Widget Doc-Comment Audit milestone — picked the second of tooltip.zig's 3 remaining unfixed doc-comment promises (session 423 fixed auto-dismiss-on-timeout; hover/focus trigger wiring and fade-in animation both still need real design work per session 423's notes): **"configurable delay before showing."** Designed the extension myself before dispatching (two-phase state machine reusing the exact `timeout_ticks`/`ticks_remaining` tick-based-timer idiom session 423 established, just for a second, distinct pending-phase countdown) and wrote the full spec into `.claude/scratchpad.md` before dispatching `test-writer`, rather than leaving the design to the agent — kept the RED-phase handoff unambiguous (test-writer's 10 tests matched the spec on the first pass, no re-dispatch needed).
  - Full TDD cycle: `test-writer` added 10 RED tests to `src/tui/widgets/tooltip.zig` (independently confirmed compile-error RED — 15 errors referencing the 4 not-yet-existing identifiers — before dispatching `zig-developer`). `zig-developer` added `show_delay_ticks: u32`/`pending: bool`/`delay_ticks_remaining: u32` fields, `withShowDelay()` builder, and reworked `show()`/`tick()`/`hide()`: `show()` with `show_delay_ticks > 0` now enters a pending phase (`visible` stays false) instead of showing immediately; `tick()` has two phases — while `pending`, counts down `delay_ticks_remaining` and only flips to `visible=true` (starting the timeout countdown at that exact point, not at the original `show()` call) once it hits 0; `hide()` also cancels an in-progress pending show. Default `show_delay_ticks=0` preserves the original immediate-show behavior for every existing caller/test — same "0 = disabled, opt-in via builder" convention as `withTimeout`.
  - Independently verified before committing (per the project's standing "don't trust agent self-reports" practice): reviewed the full `git diff` (227 insertions, only source + new tests, zero tampering with pre-existing tests), reran `zig build test` from a clean `.zig-cache` myself (exit 0, 0 failures, 81 tests total in the file). Committed (fdb08ff) + pushed.
  - **Release judgment**: single `feat:` commit, not a milestone completion — correctly did not force a release. This is now 2 consecutive unreleased feat commits on main since v2.97.0 (session 423's timeout fix + this session's delay-before-showing), both closing the same tooltip.zig doc-comment-gap pattern — worth reconsidering bundling once a 3rd lands, per the repeatedly-cited precedent, though 2 is still below that threshold on its own.
  - **Process insight**: writing the full design spec into the scratchpad myself (not delegating design to test-writer) before the RED dispatch continues to pay off, same lesson as session 423 — when the orchestrator resolves the API shape up front by finding/reusing an exact in-codebase precedent (here: `timeout_ticks`/`ticks_remaining`, extended rather than reinvented), the test-writer→zig-developer handoff needs zero re-dispatches. This is now 2 consecutive tooltip.zig cycles with a first-pass-clean TDD loop.
  - **Current state**: `build.zig.zon` reads 2.97.0, latest tag v2.97.0 (matches). 0 open issues. 1 active milestone (v2.98.0, in progress — 2/4 original tooltip.zig gaps fixed, 2 remain — hover/focus trigger wiring and fade-in animation, both flagged as needing real design work — plus an untriaged splitpane.zig drag-handle candidate plus 145+ widget files not yet swept at all). 2 unreleased feat commits on main. Next session should pick one of the 2 remaining tooltip.zig promises (trigger wiring needs an external notify-hover/notify-focus API design since the library owns no mouse/focus event loop; fade-in animation needs a frame/alpha concept with no existing precedent widget), triage splitpane.zig's drag-handle claim, or run a fresh Explore pass over more widget files.

✅ **Session 423** — FEATURE MODE (2026-08-31, counter=423). CI green (last 3 runs on main all success), 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Two pieces of work:
  1. **Released v2.97.0** (MINOR): session 422 left 4 feat commits + 1 test commit accumulated since v2.96.1 (sessions 418-422, all closing the same doc-comment-promised-but-unimplemented gap pattern in repl.zig/async_loop.zig/fmt.zig), with memory explicitly flagging this as past the bundling threshold. Verified `zig build test` (0 failures) and confirmed CI's cross-compile job (`.github/workflows/ci.yml`, 6-target matrix) is part of the green CI runs — did not re-run cross-compile locally (FEATURE mode, not stabilization, per Test Execution Policy). Followed session 410's precedent (3 feat commits bundled as a MINOR, not patch, since none were `fix:` commits) rather than defaulting to patch. Tagged v2.97.0, GitHub release created, migration issues filed (zr#150, zoltraak#116, silica#131), Discord notified. **Correction while updating docs**: the milestones.md heading previously labeled "v2.97.0 — Infinity Safety Audit" was a stale pre-assigned number from session 410 that was actually released as v2.96.1 (patch) at session 415 — renamed that heading to remove the version number and avoid confusion with this session's real v2.97.0, since CLAUDE.md's own rule is "마일스톤은 이름(테마)으로 관리, 버전 번호는 릴리즈 시점에 결정" (milestone-number pre-assignment is non-binding).
  2. **Established v2.98.0 "Widget Doc-Comment Audit" milestone** (0 active milestones after the release, feature-request/consumer-feedback issue sources empty across all 4 repos → fell to source #4 기술 부채, per the establishment process) and immediately started work on it: dispatched an Explore agent over `tui/widgets/*.zig` (145+ files, never swept by this audit method before) using the same "doc comment promises X, grep shows X never wired" method that found repl.zig/async_loop.zig/fmt.zig's gaps. Found `src/tui/widgets/tooltip.zig`'s doc comment promising 4 things never implemented: configurable hover/focus/manual triggers (`Trigger` enum declared but zero references anywhere outside its own declaration), automatic dismissal on timeout, configurable delay before showing, and fade-in animation — only manual `show()`/`hide()` existed. Deliberately scoped this cycle to exactly ONE of the four (timeout-based auto-dismiss) rather than bundling all 4, per the "one focused task per cycle" rule — the other three are noted in `docs/milestones.md`'s v2.98.0 checklist as follow-ups, along with a secondary candidate (`splitpane.zig`'s "drag handle" mouse-resize claim, not yet triaged).
  - Ran the full TDD cycle for the timeout fix: found and reused an exact precedent already in the codebase (`toast_manager.zig`'s `tick()`/`ticks_remaining: u32` pattern — 0 = persistent, decremented once per external `tick()` call, no wall-clock/global state) rather than inventing a new timing idiom. `test-writer` wrote 14 tests first (concrete field/state assertions, not vacuous) — independently confirmed RED via a clean compile-error grep before dispatching `zig-developer`. `zig-developer` implemented 2 new fields (`timeout_ticks`, `ticks_remaining`), 1 new builder (`withTimeout`), 1 new method (`tick()`), and a 1-line addition to `show()` to reset the countdown — independently re-read the full diff (confirmed zero test-code changes, matches the scoped contract exactly) and reran `zig build test` from a clean `.zig-cache` myself (exit 0, 0 failures) before committing. Committed (84aa3e6) + pushed.
  - **Release judgment**: single `feat:` commit on top of a just-released v2.97.0 — correctly did not force another release this session; left `docs/milestones.md`'s "Unreleased on main" note for the next session to decide whether to accumulate further widget-audit fixes or bundle now.
  - **Process insight**: reusing an existing in-codebase precedent (toast_manager.zig's tick-based timer) instead of designing a novel API for tooltip.zig's timeout kept the scratchpad handoff to test-writer unambiguous — the RED tests test-writer wrote matched the proposed contract almost exactly on the first pass, no re-dispatch needed. Also worth noting: of the 4 doc-comment promises found in one file, only 1 (timeout) had a ready-made codebase precedent to copy; the other 3 (hover/focus trigger detection, show-delay, fade-in) would each need real design work (trigger detection in particular needs a caller-facing notify API since the library owns no mouse/focus event loop) — expect future v2.98.0 cycles on this same file to take longer than this one did.
  - **Current state**: `build.zig.zon` reads 2.97.0, latest tag v2.97.0 (matches). 0 open issues. 1 active milestone (v2.98.0, in progress — 1/4 tooltip.zig gaps fixed, 3 remain plus an untriaged splitpane.zig candidate plus 145+ widget files not yet swept at all). 1 unreleased feat commit on main (this session's tooltip.zig fix). Next session should continue the v2.98.0 audit — pick one of the 3 remaining tooltip.zig promises, triage splitpane.zig's drag-handle claim, or run a fresh Explore pass over a fresh batch of widget files not yet checked.

✅ **Session 422** — FEATURE MODE (2026-08-30, counter=422). CI green, 0 open issues anywhere. Working tree clean at start (sessions 420/421 landed real work — see backfill note below — but left no memory entries, so this session first reconstructed what they did from `git log`/`git show` before proceeding). Continued the doc-comment-vs-implementation audit that 418/419 recommended extending to the remaining core modules: dispatched an Explore agent over progress.zig, fmt.zig, arg.zig, term.zig, color.zig, and all 5 tui/ core files (tui.zig, buffer.zig, layout.zig, style.zig, symbols.zig — explicitly excluding repl.zig, already fully audited, and all tui/widgets/*, out of scope for this pass). Found exactly one genuine gap: `src/fmt.zig`'s top doc comment (lines 3/10) and `Mode` enum (line 18-23, `plain` variant) promised a "Plain: simple key-value pairs" output format that was never implemented anywhere in the file — `Mode` itself was never referenced by any dispatcher either. All 9 other files audited clean (progress/arg/term/color/tui.zig's RenderHooks/buffer diff-optimizations/layout Constraint variants/style/symbols all genuinely wired, not just declared).
  - Full TDD cycle: `test-writer` added 7 RED tests (`src/fmt.zig:1260-1367`) against a `Plain(comptime WriterType: type)` API mirrored on the existing `Csv`/`JsonArray`/`JsonObject` streaming-writer pattern — confirmed failing with `error: use of undeclared identifier 'Plain'` before dispatching `zig-developer`. `zig-developer` implemented `Plain` (`src/fmt.zig:552-572`, 20 lines: `init(writer)` + `writeField(key, value)` writing `"{key}: {value}\n"`, no escaping per the doc comment's "simple" framing). Independently re-verified: `git diff src/fmt.zig` read in full before committing (133 insertions, exactly the 2 additions expected — no test tampering), `zig build test` re-run from scratch (exit 0, no failures). Committed `cd9a034` + pushed.
  - **Release judgment**: single `feat:` commit closing a doc-comment gap, not a completed milestone, no bug-labeled issue — per protocol, correctly skipped release. This is now the 4th consecutive unreleased feat commit on main since v2.96.1 (418 word-jump/kill-line, 419 validator wiring, 421 decodeEventBytes/readTerminalEvent, 422 this session's Plain formatter) plus one test-only commit (420, Bar glyph-width/ETA coverage) — next session should strongly consider bundling these into a release; 4 feature completions is well past the "2-3 consecutive" bundling precedent cited in prior sessions, even though each individually was judged too small on its own.
  - **Backfill for sessions 420/421 (no memory entry was left at the time — reconstructed from `git log`/`git show` this session)**: Session 420 (`64f654d`, test-only) added 126 lines of coverage for `progress.zig`'s `Bar` widget — `filled_width` rounding at 25%/50%, ETA suppression at 0%/100%, and seconds/minutes/hours ETA formatting branches, all previously untested. Session 421 (`993ad53`, feat) fixed a real bug in `src/tui/async_loop.zig`: the doc comment promised "Event handling (keyboard, mouse, resize)" but `readTerminalEvent()` always returned `null` — implemented `decodeEventBytes(bytes: []const u8) ?Event` (single-byte codes, printable ASCII, ctrl+letter, Alt+letter via ESC-prefix, arrow keys/Home/End via CSI, F1-F12 via SS3/CSI-tilde) and wired it into `readTerminalEvent()` for both blocking (250ms poll loop) and timeout modes, with a short 10ms follow-up-byte wait to distinguish Alt+key from incomplete CSI sequences. 40 new tests, all passing. This is the same "doc comment promises feature, wiring never happened" pattern as 418/419/422 — worth noting this pattern has now been found in repl.zig (x2), async_loop.zig, and fmt.zig across 4 consecutive sessions, suggesting it's a systemic artifact of this codebase's history (docs written aspirationally ahead of implementation) rather than isolated oversights. **Process gap to fix**: sessions 420/421 skipped the memory-update end-of-cycle step entirely — future sessions should verify the previous session's HEAD commit actually has a matching memory entry before assuming continuity, not just check `git log` for green CI.
  - **Current state**: `build.zig.zon` still 2.96.0, latest tag v2.96.1. 0 open issues anywhere. 0 active milestones. 4 feat + 1 test commit unreleased on main since v2.96.1. The core-module doc-comment audit is now believed exhausted (10/10 non-widget, non-repl files checked clean or fixed) — next milestone-mining session should either (a) bundle the overdue release, or (b) extend the same audit technique to `tui/widgets/*.zig` doc comments (explicitly out of scope for this session), which has 145+ files never swept this way.

✅ **Session 419** — FEATURE MODE (2026-08-29, counter=419). CI green, 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Followed session 418's exact suggested lead — grepped core module (non-widget) doc comments for promised-but-unimplemented behavior via a dedicated Explore agent — and found the next instance of the same pattern: `src/repl.zig`'s own doc comment (line 11) claims `Multi-line input: validator callback support`, and the types (`Validation` enum, `Config.validator: ?Validator`) were fully declared, but `handleKey()`'s Enter branch (`'\r', '\n' =>`) never called `self.config.validator` — it unconditionally wrote `"\r\n"` and returned `true`, so setting a validator had zero effect. This is architecturally the same class of gap as 418's kill-line/word-jump fix (a doc comment describing a feature whose supporting types exist but were never wired into the actual key-handling logic).
  - Ran the full TDD cycle: `test-writer` added 8 tests (6 initially reported + 2 helper-validator scenarios) asserting the exact contract — no validator or `.complete` → submit immediately (return `true`, unchanged from today); `.incomplete` → insert `\n` at cursor, advance cursor past it, echo `config.continuation_prompt`, return `false` (keep reading, buffer accumulates multi-line content with embedded newlines); `.invalid` → clear buffer, reset cursor to 0, reprompt with `config.prompt`, return `false` (discard the whole attempt, mirrors the existing Ctrl+C reset). Independently confirmed all 6 core tests failed for the right behavioral reason (Enter always returning `true`, no accumulation) before dispatching `zig-developer`.
  - `zig-developer` implemented the 3-way `switch` on `Validation` inside the Enter branch (32 net new lines, `src/repl.zig:299-334`), reusing the existing `self.buffer.replaceRange`/`self.printPrompt` idioms already used elsewhere in the file for consistency. No test-writer bugs surfaced this cycle (first TDD cycle in several sessions with zero re-dispatches needed). Independently verified myself before trusting the self-report: `git show <commit> --stat` confirmed the actual diff (299 insertions, 1 file), `zig build test` re-run from scratch (exit code 0, no failures). Committed (e3f4479) + pushed.
  - **Release judgment**: same as session 418's precedent — a single `feat:` commit closing a doc-comment gap, not a completed milestone, no bug-labeled issue driving it. Per protocol (`fix:`-only → patch, milestone-complete → minor, neither → skip), correctly skipped release. This is now 2 consecutive unreleased feat commits sitting on `main` since v2.96.1 (session 418's word-jump/kill-line fix + this session's validator wiring) — next session (especially if it lands a 3rd, per the repeatedly-cited "3rd consecutive fix" bundling precedent from sessions 384/391/etc., though that precedent was for `fix:` not `feat:` — worth judgment-calling whether 2 feature completions this closely related, both closing the same repl.zig doc-comment-gap pattern, justify a bundled minor/patch release even without a formal milestone) should reconsider bundling a release.
  - **Compressed `.claude/memory/project-context.md`** this session (was 181 lines, over the 200-line soft budget on the next natural append) — condensed the detailed sessions 372-391 entries into one summary paragraph preserving the load-bearing recurring lessons (verify-wiring-independently, weak-assertion audits mask real bugs, checked-box-isn't-a-release, not-every-panic-is-a-violation), dropping only the individually-resolved bug specifics from that span (already fixed, unlikely to recur in the exact same shape).
  - **Process insight**: the "grep core module doc comments vs. actual implementation" lead-generation method (first used reactively in 416-418 for repl.zig's word-jump claim, now used proactively via a dedicated Explore agent) is repeatable and cheap — worth treating as a standing milestone-establishment input alongside `gh issue list --label feature-request`, ahead of "propose widget #146" given 145+ widgets already exist with no demand signal. The same Explore pass also flagged a second, weaker gap (repl.zig's tab-completion doc comment claiming "popup menu" when only common-prefix insertion is implemented) — deliberately NOT fixed this session since the code already self-documents the scope limitation as intentional (CompletionPopup widget exists separately for TUI-integrated consumers), not an accidental gap like the validator wiring was.
  - **Current state**: `build.zig.zon` still 2.96.0, latest tag v2.96.1. 0 open issues anywhere. 0 active milestones. 2 unreleased feat commits on main (418, 419). Next session should decide on a bundled release for these two repl.zig completions, or continue mining core-module doc comments (progress.zig, fmt.zig, arg.zig, term.zig, tui/*.zig not yet swept this way) for the next gap.

✅ **Sessions 394–419** (compressed) — Released v2.92.0→v2.96.1: Rating widget, Repl bracketed-paste wiring, arg.zig subcommand dispatch + Levenshtein "did you mean", u16-overflow saturating-arithmetic sweep across ContextMenu/Block/ToastManager (8+ sites), Infinity/NaN safety audit across 34 chart widgets (found `@intFromFloat(NaN)` does NOT panic on this toolchain, only `±Infinity` does — self-cancelling Infinity/Infinity divisions in 5 widgets produce NaN, not a real bug), repl.zig word-jump/kill-line + multi-line validator wiring (doc-comment-vs-implementation gap pattern, first instance).
  - **Recurring lessons carried forward** (still load-bearing): (1) verify wiring/test-count self-reports independently (grep `sailor.zig`/`tui.zig`/`build.zig` for the claimed export) — caught real gaps repeatedly. (2) A checked `[x]` milestone box or version bump commit is not proof a release happened — always cross-check `build.zig.zon` version against `git tag -l 'v*'` (recurred a 3rd time at session 405 alone). (3) TDD-agent-boundary rule (zig-developer refuses to fix a wrong test, re-dispatches test-writer) held reliably across every cycle in this span, including 2 re-dispatches in a single cycle (session 409) — trust it, don't loosen it. (4) The "doc comment promises X, grep shows X never wired" pattern (first found in repl.zig here) became the seed for the later v2.98.0-v2.100.0 Widget Doc-Comment Audit rounds. (5) Infinity-vs-NaN reachability must be verified empirically per-toolchain before writing a "does not panic" regression test — a technically-passing-both-ways test proves nothing.
✅ **Sessions 372–391** (compressed) — Widget/tech-debt fix streak, v2.86.0→v2.94.2. Widgets shipped: ToggleSwitch(374), CalendarHeatmap(377, found uncommitted from a prior session), DonutChart(380, found unregistered in `build.zig` — a green `zig build test` had proved nothing since the suite never actually ran), IcicleChart(372, found uncommitted with 2 real bugs — focused-path index-chain validation and a hardcoded `show_values` TODO placeholder). Bug fixes: FlowChart edge/node z-order + ToggleSwitchGroup missing group-style application (375), `sparkline.zig` u64 multiply-overflow in bar-scaling math widened to u128 (388, completing a prior session's profiler work), `particles.zig` (widget) unclamped position cast to i32/u16 reachable via public `setGravity`/direct `Particle` construction (389), `term.zig` fd=42 mock-routing missing an existence guard (386), `validation.zig` `unreachable`-on-non-preset-length + global mutable message buffers (384), `validation.zig` `CombinedValidator` ring-buffer wraparound + use-after-free via unowned slice reference (385, required a `ctx`-pointer API change), `LineBuilder` `@panic`-on-OOM rule violation (382), `countdown_timer.zig` `@panic`-on-buffer-overflow in mm:ss/hh:mm:ss formatters (383), `toast_manager.zig` unguarded u16 addition overflow (391). Widened weak-assertion audit (`expect(x>=0)` on unsigned types, `expect(true)` placeholders, `<claim> or countNonEmptyCells(...)>0` disjunctions) across ~15 more test files (381), no new bugs found that pass (confirmed some strengthened-assertion failures were legitimate degenerate-case designs, not bugs — verify against source before accepting a test-writer's "regression" guess).
  - **Recurring lessons carried forward from this span** (still load-bearing for later sessions): (1) "uncommitted but `zig build test` green" is not proof of completeness — verify a new test file is actually registered in `build.zig`, and independently grep claimed wiring edits (`sailor.zig`/`tui.zig`/`build.zig`) rather than trusting an agent's self-report (sessions 361/364/380 each caught a real gap this way — this is why every session since verifies commits/diffs/test runs itself). (2) Weak/tautological assertions reliably mask real bugs once strengthened — surfaced genuine bugs in BoxPlot/FlowChart/ToggleSwitchGroup/IcicleChart, the concrete justification for CLAUDE.md rule 15. (3) A checked `[x]` milestone box is not proof a release happened — always cross-check `build.zig.zon`'s version against `git tag -l 'v*'` (caught stalled releases 3 times: sessions 354/380/405). (4) Not every `@panic`/`unreachable` is a violation — distinguish documented opt-in assert utilities (`stack_trace.zig`) from accidental landmines reachable via innocuous-sounding public APIs (`validation.zig`'s old `minLength`). (5) The recurring "unclamped float-to-int cast" bug class took ~7 files/6 sessions to exhaust (357-389); a sibling "unguarded u16/usize addition overflow" class was opened at session 391 and continued into later sessions.

✅ **Session 370** — STABILIZATION (2026-07-15). CI green, 0 issues — continued session 360's weak-disjunction-assertion audit (`or countNonEmptyCells(...) > N`) across mindmap/radar_chart/bracket_viewer/wordcloud, fixed 9 instances (bracket_viewer audited clean). Full 6-target cross-compile verified. No release (test-only changes).

✅ **Sessions 363–369** (compressed) — Released v2.86.0–v2.91.0: ParetoChart, SlopeChart, RidgelinePlot, BumpChart, MosaicPlot, IcicleChart. Recurring pattern: several of these were found as prior-session fully-implemented-but-uncommitted work (363/369) — always independently verify via `zig build test` + grep the claimed wiring edits (`src/sailor.zig`/`src/tui/tui.zig`/`build.zig`) before trusting an agent's self-report of test count or wiring completeness (gaps found in sessions 361/364). Session 364 fixed a red Windows CI flake (exact-boundary timing assertion in `profiler.zig` needed ~10% tolerance) before feature work, per protocol (CI-red overrides mode).

✅ **Session 361** — Released v2.84.0 (BulletChart). Process insight: agent claimed `sailor.zig` exports were wired but grep showed they were missing — always verify independently.

✅ **Session 360** — STABILIZATION: found the `countNonEmptyCells(...) > 0` disjunction anti-pattern (masks real bugs, e.g. session 354's BoxPlot) in 26 test files; fixed flowchart this cycle, remainder completed session 370. Released patch v2.83.1.

✅ **Session 357** — Released v2.83.0 (CandlestickChart). Fixed a shipped panic: unclamped OHLC values overflowed `@intCast` on row mapping — clamp both the normalized ratio AND the final row index, not just the lower bound.

✅ **Session 354** — Completed a stalled v2.81.0 release (checklist was `[x]` but version/tag never bumped — a checked box isn't proof, verify against `build.zig.zon` + `git tag -l`) + released v2.82.0 (BoxPlot). Caught a real bug pre-commit: box-drawing `row_q1 <= row_q3` comparison was backwards, masked by weak `countNonEmptyCells > 0` tests.

✅ **Session 352** — Released v2.80.0 (ViolinPlot: mirrored density silhouette, global min/max binning).

✅ **Sessions 346–351** (compressed) — Released v2.75.0–v2.79.0: WaterfallChart, FunnelChart, DotPlot, RadialBar, StreamGraph. Recurring gotchas: `@floatFromInt` in struct literals needs explicit `@as(f32, ...)` in Zig 0.15.x; symmetry assertions must exclude the label column; pin macOS ARM64 CI runner to macos-15 (macos-latest→26 broke Zig 0.15.2 linking).
