✅ **Session 418** — FEATURE MODE (2026-08-29, counter=418). CI green, 0 open issues anywhere. Working tree was **not** clean at start — inherited an interrupted-mid-cycle chain from sessions 416/417: session 416 hit a host-level Bash `ENOSPC` outage (temp-output capture path ran out of disk, unrelated to the repo) right after `test-writer` wrote 16 new RED tests to `src/repl.zig` for Ctrl+U/Ctrl+K/Ctrl+W kill-line/kill-word and Alt+B/Alt+F word-jump — filling a real gap where `repl.zig`'s own doc comment promised "word jump, kill line" line editing that `handleKey()` never implemented. Session 417 (Bash restored) fixed two self-inconsistent Alt+B test expectations and stripped ~300 lines of leftover reasoning-comments from the test file, leaving the tests correctly RED but still uncommitted. This session verified the tests were algorithmically self-consistent (independently re-derived the canonical backward-word algorithm against both the Ctrl+W and Alt+B cases before trusting the prior sessions' notes), then dispatched `zig-developer` for the Green phase: added `wordStartBefore`/`wordEndAfter` helpers plus the 5 new key handlers (0x15/0x0B/0x17 in the single-byte switch, a new 2-byte `ESC`+letter branch for Alt+B/Alt+F). All 12 previously-RED tests plus the full suite passed on first run; committed and pushed as `2ec0ef7`. Marked the `ENOSPC` blocker resolved in `.claude/memory/debugging.md` (was purely environmental, cleared on its own between sessions — no repo-side fix needed, treat any recurrence as transient).
  - **Milestone/release check**: 0 active milestones (checked feature-request issues across sailor/zr/zoltraak/silica — all empty; PRD.md has no unimplemented items). This repl.zig fix doesn't meet the release bar on its own (feat commit, not a completed milestone, no bug-labeled issue driving it) — per the release-judgment protocol (`fix:`-only → patch, milestone-complete → minor, neither → skip), correctly skipped release this cycle rather than force one. Left unreleased on main for a future session to batch with the next feature or milestone.
  - **Process insight**: this is now a 3-session relay (416 wrote tests → 417 fixed test bugs → 418 implemented) surviving a genuine environment outage cleanly, because each session left a durable trail in both `.claude/scratchpad.md` (ephemeral, but session 417/418 both found it still present) and `.claude/memory/debugging.md` (durable). Worth noting the scratchpad survived 2 session boundaries despite being nominally "reset each cycle" — the reset only happens when a new cycle's orchestrator explicitly overwrites it, so it's safe to rely on for a genuinely interrupted (not cleanly finished) cycle.
  - **Current state**: `build.zig.zon` still 2.96.0, latest tag v2.96.1. 0 open issues. 0 active milestones — source #4 (tech debt) from the establishment process remains unexplored across several sessions now; next session mining for a new milestone theme should look there (e.g. grep other module doc comments for promised-but-unimplemented behavior, the same pattern that surfaced this session's repl.zig gap) rather than re-checking feature-request issues/PRD, which have been confirmed empty for multiple sessions running.

✅ **Session 415** — STABILIZATION (2026-08-28, counter=415). CI green, 0 open issues/feature-requests anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Two focused pieces of work:
  1. **Patch release v2.96.1**: session 414 left one real unreleased fix commit (13cc6c5, the 4-widget Infinity/NaN guard fix) sitting on main since v2.96.0. Verified `zig build test` (0 failures) and all 6 cross-compile targets sequentially (Linux x86_64/aarch64, macOS x86_64/aarch64, Windows gnu/msvc — no concurrent `zig build` process per `pgrep` check first), confirmed 0 open bug issues, then released v2.96.1 (tag-only, `build.zig.zon` untouched per patch protocol). Filed consumer migration issues (zr#149, zoltraak#115, silica#130), Discord notified, `docs/milestones.md` updated (v2.97.0 milestone's last unchecked release checkbox closed, "Unreleased on main" cleared to none).
  2. **Test coverage audit** (mandatory stabilization step): dispatched a research-only agent to find fresh coverage gaps, explicitly steered away from the exhausted u16-overflow/`@intFromFloat`-NaN/vacuous-assertion wells. Found 3 genuine gaps in widgets untouched by the last 20 commits — builder-pattern setters whose effect on `render()` output was never asserted anywhere: `Checkbox.withCheckedStyle`/`withFocusedStyle` (untested precedence: `focused_style` should win over `checked_style` when both apply), `StreamingTable.withBlock`/`withHeaderStyle`/`withRowStyle`/`withSelectedStyle` (none of its 10 existing tests call these), `Canvas.clearDot` (single-dot erase, distinct from `clear()`, zero coverage). Dispatched `test-writer`, which added 6 tests asserting concrete rendered cell content (specific `style.fg`/`style.bold`/`char` at computed positions) — confirmed all 3 widgets behave correctly as designed, no source bugs found, pure coverage gap as expected.
  - **Caught one real test-quality issue before committing**: the initial `StreamingTable.withBlock renders border` test included a vacuous tautology (`header_cell.char != ' ' or header_cell.char == ' '`, always true regardless of behavior) alongside its one meaningful border-char assertion. Per the TDD-agent-boundary rule, re-dispatched `test-writer` to fix its own test rather than patching it myself — replaced with a concrete `expectEqual('D', header_cell.char)` check (first letter of the "Data" header, only present at that position if the block's inset logic actually ran). Independently re-verified the diff and reran `zig build test` (exit 0) myself before committing.
  - **Process insight**: this is now the Nth time (sessions 394/395/405 precedent) that independently re-reading a test-writer diff before committing — not just trusting its "all tests pass" self-report — caught a vacuous assertion that would have silently weakened the test suite. The specific shape this time (`X != v or X == v`, a full-domain tautology hidden next to one legitimate assertion in the same test) is subtler than the previously-seen "compares full buffers including irrelevant labels" or "asserts default-initialized-not-zero sentinel" shapes — worth keeping in mind that a test can have ONE good assertion and ONE vacuous one in the same block, so reviewing per-assertion, not just per-test, matters.
  - **Current state**: latest release v2.96.1 (tagged + GitHub release, this session). `build.zig.zon` still 2.96.0 (patch releases don't bump it). 0 open issues anywhere. 0 active milestones — checked all 5 milestone-establishment input sources this session (feature-request issues: empty across all 4 repos; PRD.md: no unimplemented items, project is deep post-v1.0 with 145+ widgets far beyond original PRD scope; consumer feedback issues: none; Zig version: pinned 0.15.2 matches installed toolchain, no update needed) — tech debt (source #4) is the only remaining unexplored input, deliberately left unexplored this session rather than rushing a low-effort milestone; next session (ideally feature mode, since milestone-drafting is a design task) should either mine tech debt for a milestone theme or accept this project may not need a new milestone soon given its maturity.

✅ **Session 414** — FEATURE MODE (2026-08-28, counter=414). CI green, 0 open issues anywhere. Working tree clean at start. Continued session 412's v2.97.0 Infinity Safety Audit milestone (4/34 widgets fixed, 30 untriaged). Manually traced reachability for all 30 remaining widgets myself (not delegated — this was fast, targeted grep+read work, not a multi-file implementation task) before dispatching any agent, per the milestone's own corrected methodology (trace each `@intFromFloat` call site's actual reachability from a public API through any existing clamps, don't trust a raw grep hit). **Result: zero new reachable panics found.** Every site falls into one of three safe patterns: (1) explicit pre-cast clamp already present (`gantt_chart.zig`'s `@max(0, @min(x, width))` ordering, `radar_chart.zig`'s `@min(@max(value,0),1)`, `bubble_chart.zig`'s `formatF32Into`'s `±999_999_999` clamp); (2) purely geometric float derived from integer area/index dimensions and bounded trig, never touching user data (`chord_diagram.zig`, `donut_chart.zig`, `piechart.zig`, `radar_chart.zig`'s axis-line block, `sunburst_chart.zig`, `wordcloud.zig`, `scrollview.zig`, `countdown_timer.zig`); (3) a **non-obvious self-cancelling pattern** in 5 widgets (`waterfall_chart.zig`, `stream_graph.zig`, `sankey.zig`, `mosaic_plot.zig`, `icicle_chart.zig`) — each computes a running total/max/range in a first pass that includes the same public field a caller could set to Infinity, so a later division's numerator AND denominator both become Infinity in lockstep, producing `Infinity/Infinity = NaN` (not raw Infinity) — and `@intFromFloat(NaN)` doesn't panic on this toolchain (session 410's finding). Pattern (3) is fragile (a refactor decoupling the two passes could reintroduce a real panic), so dispatched `test-writer` to add 5 regression tests locking in the property — all 5 passed GREEN on first run, confirming my analysis was correct (no source bug, no fix needed). Independently re-verified with a clean `zig build test` myself (exit 0) and reviewed the actual diff before committing — the tests assert concrete consequences (non-empty cell counts, or exact zero-cell count for stream_graph) rather than bare `expect(true)`, consistent with the project's anti-vacuous-test rule. Committed test additions (0c5d08d) and milestone/debugging doc closeout (95f2df4) separately, pushed both.
  - **Closed the v2.97.0 milestone** (`docs/milestones.md` now marks it Complete) — full triage done: 4/34 fixed (session 412), 30/34 confirmed already-safe (session 414, 5 of those backed by new regression tests). Updated `.claude/memory/debugging.md`'s long-running Infinity/NaN section with the three-pattern taxonomy so future audits of this bug class recognize self-cancelling division sites instead of re-flagging them as bugs. No source/behavior changes this session (test-only) — did not force a release, since the milestone doesn't require one on its own; the 4 source fixes from session 412 remain the only unreleased-but-real code change, still pending the next stabilization session's cross-compile check.
  - **Process insight**: doing the reachability triage myself (grep + read, ~15 files, no agent needed) before delegating was much faster and cheaper than dispatching a triage agent, since the actual judgment calls (does this division self-cancel? is this float geometric-only or data-derived?) required tracing specific call sites one at a time with tight back-and-forth — well-suited to direct tool calls, not agent delegation. Only delegated the mechanical, well-specified final step (write 5 tests against a fully-resolved scenario spec) to test-writer, per TDD-agent-boundary convention. This suggests future audit-style triage work (as opposed to open-ended implementation) may be more efficient done directly rather than defaulting to sub-agent dispatch.
  - **Current state**: `build.zig.zon` still 2.96.0, latest tag v2.96.0 (unchanged — test-only milestone completion, not a release trigger). 0 open issues. 0 active milestones (v2.97.0 now complete) — next session should either replenish per the milestone establishment process, check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first per protocol, or (if next session is a stabilization session, counter=415) bundle the 4 unreleased session-412 source fixes into a v2.96.1 patch release after cross-compile verification.

✅ **Session 412** — FEATURE MODE (2026-08-27, counter=412). CI green, 0 open issues anywhere. Working tree was **not** clean at start: found uncommitted RED tests from an unfinished v2.97.0 Infinity Safety Audit cycle (no matching source fix, no commit) for 4 of the 34 flagged widgets — `box_plot.zig`, `funnel_chart.zig`, `metricspanel.zig`, `tests/widget_particles_test.zig` — each test constructing `std.math.inf(T)`/`-std.math.inf(T)` input and asserting `expect(true)` after `render()`/`update()` (a "does this panic" RED probe, matching the milestone's prescribed reachability-testing method). Verified RED first (`zig build test` — 3 of 4 test binaries crashed with signal 6, "integer part of floating point value out of bounds", at exactly the 4 flagged call sites: box_plot.zig:335 `valueToRow.calc`, funnel_chart.zig:210 `bar_width`, particles.zig:82 `Particle.update` opacity, metricspanel.zig:341 `renderSparkline` bar index). Fixed all 4 with the established `if (isNan) 0.0 else std.math.clamp(raw, 0, max)` pattern (gauge.zig/reactive.zig/splitpane.zig precedent) at each cast site. `zig build test` — 0 failures after the fix (confirmed twice).
  - **Also converted the RED tests into real regression tests** (per the project's own "placeholder `expect(true)`" anti-pattern, documented in `.claude/memory/debugging.md`): renamed away from "(RED: Infinity panic)"/"panics on..." (now misleading post-fix) to "does not panic"/"clamps..." and added concrete assertions — `countNonEmptyCells(...) > 0` for box_plot/funnel_chart (buffer actually rendered content, not just "didn't crash"), exact `buf.get(x,y).?.char` checks against the expected sparkline bar glyph for metricspanel (`'█'`/`'▁'`), and exact `opacity` value checks for particles (255 for the +Inf-ratio case, 0 for the NaN 0/0 case — rewrote the second particles test's scenario since its original `lifetime=10,max_lifetime=0` didn't actually hit the NaN path the comment claimed, only another +Inf case; the real NaN path needs `lifetime=1` so the in-function `lifetime -= 1` produces `0/0`).
  - Updated `docs/milestones.md`'s v2.97.0 checklist: marked these 4 widgets triaged+fixed with file:line detail, noted 30 of the original 34 remain untriaged. Committed (13cc6c5) + pushed.
  - **Process insight**: a prior session's uncommitted RED tests with no source fix is now a recurring "interrupted mid-cycle" shape (matches session 397's unclosed `team_create`/`team_delete` pattern, session 410's discarded-then-corrected vacuous tests) — always diff `git status` at session start against what the active milestone checklist claims is done before assuming a clean slate.
  - **Current state**: `build.zig.zon` still 2.96.0, latest tag v2.96.0 (unchanged — this is a milestone-in-progress fix, not a full release trigger by itself). 0 open issues. v2.97.0 milestone now 4/34 widgets triaged and fixed; 30 remain. Next session should continue the triage (`area_chart.zig`, `bubble_chart.zig`, `bump_chart.zig`, ... per the milestone list) using the same `std.math.inf(T)` reachability-then-clamp-guard method, and consider a patch release once a meaningful batch (or all 34) are done.

✅ **Session 410** — STABILIZATION (2026-08-27, counter=410). CI green, 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Confirmed release conditions for the 3 features accumulated unreleased since session 406 (Rating widget #407, Repl bracketed-paste #408, arg.zig subcommand dispatch #409): `zig build test` 0 failures, all 6 cross-compile targets verified sequentially (no concurrent `zig build` process per `pgrep` check), 0 open bugs. Released **v2.96.0**: bumped `build.zig.zon`, tagged, GitHub release created, migration issues filed (zr#148, zoltraak#114, silica#129), Discord notified, `docs/milestones.md` updated (all three feature sections marked `(Complete)`, release checkboxes checked).
  - **Milestone establishment + a caught vacuous-test mistake**: with 0 active milestones (≤2 threshold), re-audited session 388's flagged-but-unfixed NaN-through-`std.math.clamp` lead. Confirmed `gauge.zig`/`reactive.zig`/`splitpane.zig` (the 3 originally flagged) are already fixed with `isNan` guards. Grepped for the same unguarded `@intFromFloat` shape and found 34 more widgets — dispatched a `test-writer` sub-agent to triage reachability and write regression tests. **Caught a mistake before committing**: the sub-agent's "does not panic on NaN" tests for 3 widgets (area_chart, box_plot, bubble_chart) were vacuous — independently verified via a standalone repro that on this project's pinned Zig 0.15.2, `@intFromFloat` on a *runtime* `NaN` does NOT panic (silently returns `0`); only `±Infinity` reliably panics ("integer part of floating point value out of bounds"). Discarded those tests (`git checkout --`) before they were committed, corrected `.claude/memory/debugging.md` and the new `docs/milestones.md` v2.97.0 milestone entry to require `std.math.inf(T)` (not `NaN`) as the reachability probe for all future work on this bug class, and to flag that per-widget bounds checks may already filter raw Infinity before it reaches a given cast site (traced through `bubble_chart.zig` as a concrete example — its `if (norm_x < 0.0 or norm_x > 1.0) continue;` check already filters Infinity, so NOT every grep hit is a real bug). Established v2.97.0 "Infinity Safety Audit" milestone with the corrected, unverified-starting-point widget list and a checklist that front-loads the Infinity-vs-NaN distinction so the next session doesn't repeat the same vacuous-test mistake.
  - **Process insight**: a sub-agent's self-reported "tests currently pass, will fail once the fix lands" claim is not proof the tests are meaningful — when a test asserts "does not panic," verify independently (a tiny standalone `zig test` repro) that the exact panic condition is even reachable on the pinned toolchain before trusting the RED step. This is the same "always verify wiring/test claims independently" lesson from sessions 361/364/380/405, just applied to a new failure mode: a technically-passing-both-ways test, not a missing export.
  - **Current state**: latest release v2.96.0 (tagged + GitHub release, this session). 0 open issues. 1 active milestone (v2.97.0 — Infinity Safety Audit for chart widgets, triage not yet started for real — the 3-widget test-writer attempt was discarded). Next session should start the v2.97.0 triage using `std.math.inf(T)` probes per widget, tracing existing bounds checks first before assuming a grep hit is a real bug.

✅ **Session 409** — FEATURE MODE (2026-08-26, counter=409). CI green, 0 open issues anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Picked up the concrete lead flagged repeatedly since session 404 and never actioned since: `arg.zig`'s own module doc comment had claimed "Subcommand support" since inception but it was never implemented — session 404's Explore audit ranked it the #1 consumer-facing gap (`zr` needs `zr build`/`zr test` style dispatch), deferred at the time as "architecture-level scope creep beyond a single-cycle fix." Ran the full team protocol as CLAUDE.md mandates for architecture changes: `architect` first (designed `Commands(comptime commands: []const CommandDef)` — comptime-generic, mirrors `Parser(flags)` conventions; `.match()`/`.dispatch()` against `args[0]`; each subcommand gets its own independent `Parser(cmd.flags)` type selected via a comptime-unrolled `inline for` + index-comparison since Zig can't hold heterogeneous `Parser(flags)` instantiations in one runtime value; reuses Levenshtein "did you mean?" logic via a hoisted-to-file-scope `findClosestMatch` helper; explicitly recommended scoping OUT nested sub-subcommands as YAGNI, with a documented manual-composition escape hatch if ever needed), then `test-writer` (wrote 13 failing tests, confirmed RED — compile errors on undefined `Commands`/`CommandDef`/`CommandError`), then `zig-developer`. **Two legitimate test-writer bugs caught and correctly refused by zig-developer in the same cycle** (TDD-agent-boundary rule held twice): (1) 5 visitor `run()` methods referenced an undefined `this` identifier (Zig has no implicit `this`, only a named receiver param) — re-dispatched test-writer, fixed to `self: @This()` + `self.xxx`; (2) the "different flag sets per command" test unconditionally called `parser.get("release")` AND `parser.get("verbose")` in one generic `run()` body, but since `run(comptime cmd, parser: anytype)` monomorphizes separately per distinct `cmd` (each with a different `Parser(cmd.flags)` type missing the *other* command's flag), both branches needed a `comptime std.mem.eql(u8, cmd.name, ...)` guard — re-dispatched test-writer again, fixed correctly. zig-developer's own implementation code was never touched by either fix. code-reviewer pass: 0 critical/warning findings — traced the `inline for`+index-comparison dispatch pattern to confirm only the matched branch executes at runtime, verified `defer parser.deinit()` fires on the error-propagation path (no leak), confirmed all `suggestion` strings are comptime-known static data (no dangling pointers across `deinit()`), confirmed the `levenshteinDistance`/`findClosestMatch` extraction is a pure byte-for-byte move with zero behavior change to `Parser`'s existing public API (all pre-existing tests pass unchanged), full library-rule compliance (no stdout/panic/global-state, Writer-based `writeHelp`). 3 non-blocking suggestions noted (no zero-command `Commands(&.{})` test — traced manually as safe, not added; test visitor internals use an unnecessarily indirect pointer round-trip instead of direct field access — cosmetic; a test-count bookkeeping discrepancy, 12 vs. claimed 13, harmless). Independently re-ran `zig build test` myself three times across the cycle (after test-writer's initial RED, after test-writer's two bug-fixes, and after code-reviewer's pass) — 0 failures on the final run. Committed (0dfcdf0) + pushed.
  - **Release judgment**: deliberately did NOT bump `build.zig.zon` or cut a tag — FEATURE mode session (counter=409, not a multiple of 5), and per CLAUDE.md's Test Execution Policy, 6-target cross-compile is restricted to CI/stabilization sessions while the release protocol requires cross-compile success as a hard release condition. This is now the 3rd unreleased feature sitting on main (Rating widget session 407 + Repl paste-wiring session 408 + this session's arg.zig subcommand dispatch) — `docs/milestones.md` updated to reflect all three under the same pending v2.96.0 release note.
  - **Process insight**: the TDD-agent-boundary rule held twice in a single cycle without any prompting or friction — zig-developer correctly distinguished "this is my implementation code, I fix it" (the `comptime visitor: anytype` → `visitor: anytype` signature fix, made directly) from "this is test code with a bug, I report and stop" (both the `this`/`self` typo and the missing comptime-branch guard) on the first attempt each time, consistent with sessions 384/385/394/408's precedent. Worth continuing to trust this boundary rather than loosening it for cycle-count efficiency — 2 re-dispatches in one cycle is a normal cost of catching real bugs before they land, not a sign the rule is too strict.
  - **Current state**: `build.zig.zon` still reads 2.95.0 (unchanged), latest tag still v2.95.0. 0 open issues. Three features unreleased on main (Rating widget session 407, Repl paste-wiring session 408, arg.zig subcommand dispatch session 409) — next STABILIZATION session (counter=410, counter%5==0) should verify 6-target cross-compile, bump version to v2.96.0, tag, and release all three together.

✅ **Session 408** — FEATURE MODE (2026-08-26, counter=408). CI green, 0 open issues/feature-requests anywhere (sailor/zr/zoltraak/silica). Working tree clean at start. Picked up the concrete lead flagged in session 391's memory entry and never actioned since: `src/repl.zig`'s `Repl.handleKey()` never enabled or recognized bracketed paste mode even though `src/paste.zig` (`PasteHandler`/`PasteReader`) and `src/term.zig`'s `BracketedPaste`/`isPasteStart`/`isPasteEnd` have existed and been exported for many sessions — every `\r`/`\n` byte (including ones inside a pasted block) triggered "line complete" in the old code, so multi-line pastes into REPL-based consumers (silica's SQL shell, zoltraak's redis-cli REPL) were delivered as N separate `readLine()` calls instead of one literal string. Verified the gap first (`grep -n paste src/repl.zig` — zero hits) before starting the cycle. Full TDD: test-writer wrote 16 tests first confirmed RED (compile errors on the not-yet-existing `Config.enable_bracketed_paste`/`Repl.in_paste`/`Repl.paste_buffer`/`Repl.paste_mode` fields); zig-developer implemented `Config.enable_bracketed_paste: bool = true`, paste-mode enable-on-raw-mode-entry + disable-on-deinit, and `handleKey()` accumulating paste content across multiple `read()` chunks (pastes can exceed the 16-byte `key_buf`) via `paste_buffer`, flushing literally into `buffer` at cursor on the end marker with embedded newlines never triggering line-submit — but correctly **stopped mid-cycle and reported instead of silently fixing** a miscounted test assertion (`"hello world done"` is 16 bytes, test-writer's own test asserted cursor==15), per the TDD-agent-boundary rule. Re-dispatched test-writer to fix its own arithmetic bug (confirmed: 16 is correct), then zig-developer's implementation passed all 16 tests unchanged. code-reviewer pass: 0 critical/warning findings — recursion in the trailing-bytes-after-end-marker path is structurally bounded (each recursive call passes a strictly-shrinking slice, plus real call sites are bounded by the 16-byte `key_buf`), a stray end marker with no matching start falls through harmlessly to the existing no-op escape-sequence branch, `catch null` on `BracketedPaste.enable()` matches the file's existing non-fatal history-load/save convention (verified precedent actually exists at repl.zig:119-121/138-141), writer-only I/O confirmed end-to-end (no stdout/panic/global-state). Independently re-ran `zig build test` myself twice (before and after the review) — 0 failures both times. Committed (88838b6) + pushed.
  - **Release judgment**: deliberately did NOT bump `build.zig.zon` or cut a tag — FEATURE mode session (counter=408, not a multiple of 5), and CLAUDE.md's Test Execution Policy restricts 6-target cross-compile to CI/stabilization sessions, while the release protocol requires cross-compile success as a hard release condition. This is now the 2nd unreleased feature sitting on main (Rating widget from session 407 + this session's paste fix) — `docs/milestones.md` updated to reflect both under the same pending v2.96.0 release note.
  - **Process insight (reinforced)**: this is the Nth time an agent (zig-developer this session, same pattern as sessions 384/385/394) correctly refused to touch a test-writer test it believed was wrong and instead stopped to report — the TDD-agent-boundary rule continues to hold reliably across sessions and is worth keeping as-is rather than loosening it "for efficiency."
  - **Current state**: `build.zig.zon` still reads 2.95.0 (unchanged), latest tag still v2.95.0. 0 open issues. Two features unreleased on main (Rating widget session 407, Repl paste-wiring session 408) — next STABILIZATION session (counter%5==0) should verify 6-target cross-compile, bump version to v2.96.0, tag, and release both together.

✅ **Session 407** — FEATURE MODE (2026-08-26, counter=407). CI green (latest run success), 0 open issues anywhere. No active milestones and no feature-request issues to replenish from (still, per session 404's note — ~15+ sessions without a fresh milestone). Since 145+ widgets already exist and prior sessions have been drifting toward niche chart types, added a form-control-style widget instead: **Rating** (`src/tui/widgets/rating.zig`) — discrete star/symbol rating display (product-review convention), distinct from `RangeSlider` (continuous) and `Gauge` (percentage bar). Full TDD cycle: test-writer wrote 72 failing tests first (`tests/rating_test.zig`) establishing the exact API contract (nearest-0.5 star rounding, NaN/±Infinity-safe value clamping, max normalized to `[1,32]`, builder-pattern immutability, block-border inset, "value/max" one-decimal-place text), then zig-developer implemented against them without touching the tests. One gap in the agent's self-report caught by independent verification (per the recurring "always verify wiring claims" lesson from sessions 361/364): it claimed `sailor.zig` was updated but `grep -n Rating src/sailor.zig` came back empty — added the missing `pub const Rating = tui.widgets.Rating;` export myself before committing. `zig build test`: 0 failures (both before and after the sailor.zig fix).
  - **Release judgment**: deliberately did NOT bump `build.zig.zon` or cut a tag this session — this is FEATURE mode, and CLAUDE.md's Test Execution Policy restricts 6-target cross-compile to CI/stabilization sessions only, while the release protocol requires cross-compile success as a hard condition. Bumping version without verifying cross-compile would risk re-creating the exact "checklist says done but no tag exists" gap sessions 354/380/405 each had to catch retroactively. Left `docs/milestones.md`'s new v2.96.0 section with the release checkbox unchecked and an explicit note for the next stabilization session to pick up.
  - **Process insight (reinforced, not new)**: subagent self-reports of "wired into sailor.zig/tui.zig/build.zig" are not reliable proof — grep for the actual `pub const <Name>` line before trusting the summary, same as sessions 361/364/380 already established. This is now the 4th confirmed instance of an agent's wiring self-report being incomplete.
  - **Current state**: `build.zig.zon` still reads 2.95.0 (unchanged), latest tag still v2.95.0. 0 open issues. Rating widget committed+pushed but unreleased — next STABILIZATION session (counter%5==0) should verify 6-target cross-compile, bump version to v2.96.0, tag, and release per the deferred checklist item in `docs/milestones.md`.

✅ **Session 405** — STABILIZATION (2026-08-25, counter=405). CI green, 0 open issues. **Found and fixed a real "checklist says done but wasn't" gap** (same pattern as sessions 354/380, now confirmed to recur even after those fixes): `build.zig.zon` read `2.95.0` and `docs/milestones.md` marked the AreaChart milestone "Complete" with "Release v2.95.0" checked off (`df49b8e` bumped the version back on 2026-08-25), but **no `v2.95.0` git tag or GitHub release ever existed** — `git tag -l 'v*'`'s actual latest was `v2.94.4`. Verified `zig build test` (0 failures) and ran all 6 cross-compile targets sequentially (stabilization exception, no concurrent `zig build` process per `pgrep` check) — all green — then cut the real tag/GitHub release (bundling the already-merged `f1efdae` arg.zig "did you mean" feat commit into the same release notes since it was sitting unreleased on top), filed consumer migration issues (zr#147, zoltraak#113, silica#127), updated `docs/milestones.md`'s "Unreleased on main" line to none.
  - **Then ran the mandatory stabilization Test Coverage step**: delegated a research-only Explore-style agent to find untested public functions, explicitly steering it away from the exhaustively-audited wells (u16-overflow, `@intFromFloat` NaN-clamp, weak/tautological assertions, `@panic` violations — all confirmed dry as of session 404) toward fresh files. It found `MultiCursorEditor.setLanguage` (`src/tui/widgets/multicursor.zig:83-86`) had zero test coverage anywhere — the real gap being the never-tested *interaction* between `Editor.render()`'s tokenization/token-styling branch (`if (self.language != .none)`, editor.zig ~442-480) and `MultiCursorEditor.render()`'s secondary-cursor cell overlay (multicursor.zig 261-300, which overwrites `cell.style` after the base editor renders). Dispatched `test-writer` per the TDD-agent-boundary rule (test-only work still routes through test-writer, not the orchestrator directly): added 3 tests proving (a) `setLanguage` sets `base.language` and returns `self` for chaining, (b) render doesn't panic with a language + secondary cursor active, (c) critically — the cursor overlay's style wins over the token's keyword styling at the overlap position while the character itself is preserved uncorrupted. All 3 passed GREEN on the first pass — **no bug found**, this was a pure coverage gap, not a hidden defect. Independently re-verified `zig build test` (exit 0) and reviewed the diff (74 lines, one file, matches existing test conventions) before committing. Committed (2a9e961) + pushed.
  - **Process insight**: the "release checklist marked `[x]` but tag/release never cut" failure mode identified in sessions 354/380 is NOT a one-time bug that was permanently fixed — it recurred a 3rd time (this session) purely because a `chore: bump version` commit landed without the release step immediately following it in the same session. Worth treating "verify `build.zig.zon` version has a matching `git tag`" as a standing Phase-1-status-check item every session (not just when specifically investigating a stalled release), since it's now recurred 3 times across ~50 sessions with no session having caught it proactively — always caught retroactively by the next session's audit.
  - **Current state**: latest release v2.95.0 (tagged + GitHub release, matches build.zig.zon, this session). 0 open issues. No active milestones — next session should check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first per protocol; if still empty, consider proactively drafting a milestone (this project has gone ~15+ sessions without a new feature milestone, per session 388's note, still true) rather than defaulting to another one-off tech-debt/coverage fix.

✅ **Session 404** — FEATURE MODE (2026-08-25, counter=404). CI green, 0 open issues/feature-requests anywhere (sailor/zr/zoltraak/silica) — milestone list still empty (carried since ~session 391). Per the Milestone Establishment Process, delegated an Explore agent to find a genuinely useful CORE gap rather than another niche chart widget (138+ widgets already exist with clearly diminishing demand signal). Top finding: `src/arg.zig`'s own module doc comment has claimed "Levenshtein-based 'Did you mean?' suggestions" since inception but it was never implemented — `Error.UnknownFlag` gave zero suggestion text. Implemented via full TDD cycle (test-writer wrote 11 tests red, zig-developer implemented, code-reviewer clean pass, independently re-verified `zig build test` exit 0 myself before each handoff): `Parser(flags)` now has a `suggestion: ?[]const u8 = null` field, reset at the top of every `parse()`, set when an unknown **long** flag's name is within Levenshtein distance ≤3 of a defined flag (allocation-free, fixed 128-char DP buffer). Deliberately scoped OUT of this cycle: short-flag (`-x`) suggestions (no suggestion computed, confirmed intentional not oversight) and full subcommand support (the Explore agent's other, larger finding — `zr` needs `zr build`/`zr test` style dispatch, a genuine architecture-level change deserving its own cycle, likely via `architect` first). Committed (f1efdae) + pushed; CI triggered, not yet confirmed green as of this entry.
  - **New lead for next session**: subcommand support in `arg.zig` (`Command`/`Subcommand` type wrapping per-command `Parser(flags)` instances, dispatch on `positional[0]`) — the Explore agent's #1-ranked finding, deferred this cycle as architecture-level scope creep beyond the single-cycle "did you mean" fix. Also #2 finding (lower priority, not started): `src/repl.zig` never wires up the already-complete `src/paste.zig`/`term.BracketedPaste` bracketed-paste infrastructure — multi-line pastes in REPL-based consumers (silica SQL, zoltraak) get split into one command per line instead of inserted as literal text.
  - **Process insight**: when `gh issue list --feature-request` and PRD gaps are both dry for many consecutive sessions, delegating an Explore agent specifically instructed to look at CORE modules actually used by consumers (per CLAUDE.md's own consumer-usage table: arg/color/progress for zr, arg/color/repl for zoltraak, arg/color/repl/fmt for silica) rather than defaulting to "add widget #139" surfaced a real, demand-adjacent gap on the first try — worth repeating this framing before reaching for another chart widget.
  - **Current state**: latest release v2.95.0 (AreaChart, unchanged this session — this cycle's arg.zig change is a `feat:` commit, not yet tagged/released; next session should check whether CI confirmed green on f1efdae and decide release per protocol). 0 open issues. No active milestones.

✅ **Session 398** — FEATURE MODE (2026-08-23, counter=398). CI green, 0 open issues, 0 feature-request issues anywhere (sailor/zr/zoltraak/silica). Working tree was clean at session start (no uncommitted prior work this time, unlike the last several sessions). Picked up the concrete lead session 395 flagged and left explicitly unfixed: `src/tui/widgets/context_menu.zig:318`'s `shortcut_x` computation cast `content_area.width` (`u16`, up to 65535) to `i16` (max 32767) via `@intCast` inside `render()` — panics whenever a caller passes `render(buf, area)` a raw `Rect` with `width > 32767` directly (no requirement to route through `fittingArea()`'s screen-clamping first, matching the exact reachability shape of every prior u16-overflow widget bug this series has fixed). Reproduced empirically first via a throwaway test (`ContextMenu` with one shortcut-bearing action item, `area.width=40000` → signal 6 `@intCast` panic at line 318), reverted before starting the real cycle. Ran the full team protocol: test-writer added 4 regression tests to `tests/context_menu_test.zig` (2 boundary/normal-width non-regression checks that passed immediately, 2 above-i16-max panic tests confirmed RED); zig-developer replaced the i16-cast arithmetic with pure `u16` saturating arithmetic (`content_area.width -| shortcut_len_u16 -| 1`), proving via the existing line-317 guard (`shortcut.len + 1 < content_area.width`) that the new `@intCast(shortcut.len)` to `u16` is always safe (`shortcut.len <= 65533`) — no test-writer re-dispatch needed, all 4 tests passed as originally written. code-reviewer independently re-ran `zig build test` (green), hand-verified the guard proof and that the new formula reproduces the old i16 formula exactly for all previously-valid widths, and confirmed the diff (7 lines in context_menu.zig, one appended test block) is minimal and scoped — flagged one non-blocking redundant-but-harmless inner-guard note, not acted on. Committed (4f17e9d) + pushed.
  - **Release judgment**: only 1 fix commit accumulated since v2.94.4 (this session's) — below the repeatedly-cited "3rd consecutive fix" bundling threshold (sessions 383/386/389 precedent), and not a consumer-reported bug/CI break/cross-compile break, so per the narrower patch-release trigger conditions this does NOT auto-release. Deferred pending further accumulation.
  - **Process insight**: a previously-flagged-but-deferred lead (session 395's "New lead flagged by code-reviewer, NOT fixed this session" note on this exact line) is a reliable, cheap next-cycle starting point when `gh issue list --label feature-request` is empty across all 4 repos — it skips the "find a new bug via fresh grep" step entirely since the reproduction shape and file:line were already recorded. Worth checking prior sessions' "New lead for next session" notes before starting a fresh audit sweep.
  - **Current state**: latest release still v2.94.4 (unchanged this session). 0 open issues. No active milestones (2nd consecutive session with nothing to replenish from) — next session should check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first per protocol; if still empty, consider either a fresh `@intFromFloat`/u16-overflow grep sweep (the well may finally be dry — worth confirming) or proactively drafting a milestone per the Milestone Establishment Process, since this tech-debt-fix cadence has now run for ~15 consecutive sessions without a new feature milestone.

✅ **Session 397** — FEATURE MODE (2026-08-23, counter=397). CI green, 0 open issues. Found a prior session's uncommitted-but-complete NaN-safety fix (`team_create` "nan-safety-fix" logged in agent-activity.jsonl but never `team_delete`'d or committed): 7 widgets (`Gauge`, `SplitPane`, `ProgressRing`, `ReactiveGauge`, `RangeSlider`, `BulletChart`, `ParetoChart`) guarded against NaN reaching `@intFromFloat` — the long-running "unclamped @intFromFloat" bug well (sessions 357/372/382-389/391) finally being closed proactively via the NaN angle flagged as speculative back in session 388, rather than reactively per-file. Two stray build artifacts (`gaugenantest` empty file, `nantest` Mach-O binary) sat untracked in the repo root — removed as scratch debug leftovers, not part of the fix.
  - **Found and fixed one test bug before trusting the work**: `zig build test` failed on `bullet_chart_test.zig`'s new NaN-regression test — not an implementation bug, but the test itself compared full rendered buffers (including labels) between a bullet labeled `"NaN"` and one labeled `"Zero"`, so the mismatch was just the label glyphs differing, not a NaN-handling defect. Fixed by giving both bullets the same label (`"Test"`) so the comparison actually isolates the value-normalization path. Re-ran full clean-cache `zig build test`: 0 failures.
  - Appended the missing `team_delete` log entry for "nan-safety-fix" before committing (team_create had no matching close). Committed (833c6b2) + pushed, confirmed CI green (native tests + benchmarks + all 6 cross-compile targets) before tagging.
  - **Release judgment**: 2 fix commits (cd6734b ContextMenu overflow from session 395, 833c6b2 this session's NaN guards) had accumulated since v2.94.3 with 0 open bug issues — met the "fix: 커밋만 존재 → PATCH" trigger. Released **v2.94.4** (tag-only, `build.zig.zon` untouched per patch-release protocol). Filed consumer migration issues (zr#146, zoltraak#112, silica#124), Discord notified.
  - **Process insight**: always verify a `team_create` log has a matching `team_delete` before treating prior-session work as cleanly finished — an unclosed team entry is a cheap signal that a cycle was interrupted mid-flow (matches the broader "uncommitted-but-green work from a prior session" pattern seen repeatedly since session 372, just surfaced via the activity log this time instead of via file mtimes).
  - **Current state**: latest release v2.94.4 (tagged + GitHub release). 0 open issues. No active milestones — next session should check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first per protocol; if still empty, the `@intFromFloat`-without-NaN-guard well may finally be dry across all 7 previously-flagged widgets — worth a fresh grep to confirm before picking a new tech-debt angle.

✅ **Session 395** — STABILIZATION (2026-08-23, counter=395). CI green (e3d08cd), 0 open issues. First cut the bundled patch release session 394 left pending: **v2.94.3**, covering 5 fix commits accumulated since v2.94.2 (5fe4921 ToastManager, 11e69cc Block.inner, 5f10b9c Block.render, ddd00f2 Autocomplete Zig-0.15 migration, 788367d ContextMenu.fittingArea) — verified `zig build test` clean on HEAD first, tagged/GitHub-released, consumer migration issues filed (zr#145, zoltraak#111, silica#123).
  - **Then continued the u16-overflow audit** per session 394's flagged lead: `ContextMenu.render()`'s `content_area.x/y += 1` (context_menu.zig:261-262) — reproduced via throwaway test (`render()` with `area.x=65535` + `self.block` set → panic at line 261, signal 6), reverted, then ran the full team cycle. **Scope expanded mid-cycle**: zig-developer's first fix pass (lines 261-262 only, matching the originally-scoped lead) still panicked — test-writer's own extreme-value test cascaded into a *second* overflow at line 307, proving the two-site scope assumption from session 394 was wrong: `content_area.x` saturating to 65535 at line 261 means every downstream `content_area.x + <offset>` site in the same `render()` call (278, 307, 333, 365, 375) is reachable from the exact same single call, not a separate compounding condition as originally guessed. Widened scope to all 8 sites in the function (261, 262, 270, 278, 307, 333, 365, 375), all fixed with the established saturating `+|`/`-|` idiom. Confirmed via `Buffer.set()`'s existing bounds-check (`x >= self.width or y >= self.height` → no-op) that saturated-to-max coordinates are architecturally safe, not just panic-free.
  - code-reviewer caught one more test-quality issue before commit: the 3rd regression test's "render produced content" check used `cell.char != 0` as the emptiness sentinel, but `Buffer.init()`'s default `Cell{}` has `char = ' '` (32) not `0` — the assertion passed unconditionally even with `render()` never called (vacuous, violates rule 15). Re-dispatched test-writer, which replaced it with a specific-character assertion (expected label glyph `'S'` from "Save" at the exact computed content_area position) and empirically verified the test actually fails when `render()` is commented out before restoring it. Independently re-ran `zig build test` (green) before committing. Committed (cd6734b) + pushed.
  - **New lead flagged by code-reviewer, NOT fixed this session** (different bug shape — cast-range violation, not addition overflow): `context_menu.zig:318`'s `shortcut_x` computation casts `content_area.width`/`shortcut.len` (both `u16`/`usize`, range 0–65535) to `i16` (range ±32767) via `@intCast` — will panic for any `content_area.width > 32767`, a case not exercised by this session's fixes since content_area.width is itself bounded by the original area.width after saturating subtraction. Needs its own throwaway-reproduce-first cycle.
  - Ran the stabilization-session cross-compile check (all 6 targets, sequential, no concurrent `zig build` process) — all green.
  - **Process insight**: an agent hitting a *second* panic mid-fix, in a file/function the scratchpad explicitly scoped OUT, is not a test-writer bug to reject — it's a signal that the orchestrator's original scope boundary was drawn on an untested assumption ("these sites need compounding conditions, not yet reproduced"). When that happens, re-verify the assumption (in this case: same call, same `content_area.x` value, no separate trigger needed) and expand scope rather than dispatching test-writer to write a narrower/weaker test just to fit the original scope. The TDD-agent-boundary rule (zig-developer doesn't touch tests) still held — zig-developer correctly stopped and reported instead of narrowing the test itself.
  - **Current state**: latest release v2.94.3 (tagged + GitHub release). 0 open issues. Next session should either continue the u16-overflow sweep (candidate: `context_menu.zig:318`'s `shortcut_x` i16-cast-range issue, or grep other widgets for the same `origin_x`/`origin_y`-into-render()` reachability shape) or check `gh issue list --label feature-request` first per protocol.

✅ **Session 394** — FEATURE MODE (2026-08-22, counter=394). CI green, 0 open issues. First caught up on two prior sessions' (392-393) undocumented work landed directly on `main` with no memory entry: `Block.inner()` (11e69cc) and `Block.render()`'s renderBorders/renderTitle (5f10b9c) both got the same saturating-arithmetic (`+|`/`-|`) treatment as session 391's ToastManager fix — closing out the exact lead session 391 flagged — plus an unrelated `Autocomplete` widget Zig-0.15-unmanaged-ArrayList migration (ddd00f2). Verified all three independently via `zig build test` (green) before treating them as trustworthy.
  - **New work this session**: widened the same u16-overflow audit lens started in session 391 across the widget catalog (grepped `src/tui/widgets/*.zig` for raw `.x +`/`.y +`/`.width +`/`.height +` outside the already-fixed `+|` idiom — found the pattern in ~40+ files, far too broad to mass-fix in one cycle) and picked one concrete, high-confidence instance to fix via the full TDD cycle: `ContextMenu.fittingArea()` (`src/tui/widgets/context_menu.zig`) computes `area.x = self.origin_x` / `area.y = self.origin_y` from public, unvalidated `origin_x`/`origin_y` fields (settable via the public `withOrigin(x, y)` builder), then does raw `area.x + area.width > screen.x + screen.width` (and the y/height analog) to screen-clamp the popup position — reachable and reproducible: `ContextMenu.init(&.{}).withOrigin(65535, 0).fittingArea(screen)` panics with "integer overflow" at context_menu.zig:235. Manually reproduced via a throwaway test (reverted before the real cycle) per session 384's "verify empirically before fixing" convention.
  - Ran the full team protocol: test-writer wrote 9 regression tests (confirmed RED — same overflow panic); zig-developer fixed both comparison sites with saturating `+|` (matching the Block.inner()/ToastManager idiom exactly), and **correctly refused to fix a wrong test assertion itself** (a test expected `area.height == 1` where the function's documented min-height-3 rule and the test's own comment both said 3) — re-dispatched test-writer to fix its own test per the TDD-agent-boundary rule. code-reviewer then caught a *second* test-quality issue on independent review: 5 of the 9 tests were weak/tautological (`x < 65535 or x == 65535`, always true; 3 others only asserted the untouched axis) — re-dispatched test-writer a second time to hand-derive and assert the exact clamped x/y values via the saturating-clamp formula for all 5. Final: 9/9 meaningful tests, `zig build test` exit 0. Independently verified the diff myself (2-line fix, `+|` on both comparison operators) before committing. Committed (788367d) + pushed.
  - **New leads flagged by code-reviewer, NOT fixed this session** (scope was `fittingArea()` only): `context_menu.zig` `render()`'s `content_area.x/y += 1` (line 261-262) has the same unguarded-addition shape against the public `area` param; `fittingArea()`'s own `preferred_width` accumulation loop does unguarded `+4`/`+1`/`+3` on `intCast`'d label/shortcut lengths (theoretical, needs a label within ~4 chars of 65535 to trigger). Worth a follow-up cycle.
  - **Release judgment**: 4 fix commits now accumulated since v2.94.2 (11e69cc, 5f10b9c, ddd00f2, 788367d) — past the repeatedly-cited "3rd consecutive fix" bundling threshold. Triggered CI on the push; see next session's entry (or this session's continuation) for the release outcome if CI hadn't completed before this entry was written.
  - **Process insight**: the u16-addition-overflow bug class (distinct from the long-running `@intFromFloat` cast-without-clamp well) is genuinely widespread — a fresh grep across all widget files turned up the raw-`+`-instead-of-`+|` pattern in 40+ files, not just the 3 fixed so far (toast_manager, block, context_menu). Given the scale, mass-fixing is NOT appropriate for a single cycle (violates "one focused task per cycle" + risks a large blast-radius edit with only spot-verification) — the sustainable approach mirrors the `@intFromFloat` audit's multi-session cadence: one well-verified, TDD'd instance per cycle, prioritizing widgets with public unvalidated position/origin fields (the `origin_x`/`origin_y`-style reachability shape) over pure geometry-derived internal values. Also reinforces that code-reviewer's "meaningful tests only" check is now a standing second gate on TOP of the TDD RED/GREEN boundary — this session needed two separate test-writer re-dispatches (one for a wrong assertion, one for tautological assertions) before the cycle was actually done.
  - **Current state**: latest release still v2.94.2 (pending this session's release decision once CI confirms). 0 open issues. 138 widget files (unchanged). Next session should either continue the u16-overflow sweep (candidates: `context_menu.zig render()`, or grep further for the same `origin_x`/`origin_y`-style public-field-into-raw-addition shape in other position-configurable widgets like tooltip/popup/dialog) or check `gh issue list --label feature-request` first per protocol.

✅ **Session 391** — FEATURE MODE (2026-08-22, counter=391). CI green (8ce0961 green), 0 open issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica). Found session 389's memory entry was uncommitted and session 390 (STABILIZATION, silent — never wrote a memory entry) had landed two more commits (0eba4e6 flaky-test fix, 8ce0961 layout-helper test coverage) on top, bringing the unreleased-fix count to 5 since v2.94.1 — well past the "3rd consecutive fix" bundling threshold flagged repeatedly since session 384. Cut **v2.94.2** (patch, tag-only per protocol — `build.zig.zon` untouched, still reads 2.94.0): verified CI green + `zig build test` clean on HEAD first, tagged, GitHub release created, consumer migration issues filed (zr#144, zoltraak#110, silica#122), Discord notified.
  - **Then did feature/bugfix work** (release alone isn't "writing code" per the session mandate): re-ran the exhaustively-audited tech-debt wells first (`@panic`/`std.debug.print` outside test blocks, `expect(true)` placeholders, `>= 0` tautologies on unsigned types) — all confirmed **still dry** (zero hits project-wide), consistent with sessions 389-390's findings. Checked for wiring gaps (widget files not registered in `tui.zig`, non-widget `src/tui/*.zig` modules not re-exported via `sailor.zig`'s `tui` re-export) — none found, everything is wired. Checked for a PRD/naming gap in the widget catalog (breadcrumb/toast/stepper/wizard/carousel/accordion/rangeslider all already exist) — none found; 138 widgets already cover essentially every common TUI-dashboard pattern, so did NOT add widget #139 as a hollow "have to ship something" move (would violate "no over-engineering").
  - **Found a genuine new bug class** (distinct from the exhaustively-audited `@intFromFloat`-cast-without-clamp shape): `src/tui/widgets/toast_manager.zig`'s `ToastManager.render()` did **unguarded `u16` addition** — `screen.y + screen.height` (recomputed 3x across the bottom-stacking cursor init and the visibility/avail_height checks) and `inner.x + inner.width` (recomputed 4x in the icon/message rendering block) — both overflow u16's max (65535) when a caller passes a `Rect` with extreme y/height (e.g. `y=65530, height=100`, a public struct field with zero validation, same reachability shape as every prior "extreme value" widget bug). Manually reproduced via a throwaway test appended to `tests/toast_manager_test.zig` (`zig build test` → signal 6 / "integer overflow" panic at line 154), reverted before starting the real TDD cycle. Ran the full team protocol: test-writer added 6 regression tests (confirmed red — same overflow panic); zig-developer fixed all 7 unguarded-addition sites using saturating arithmetic (`+|`/`-|`, matching the idiom already used elsewhere in the same function for `cur_y +|= ...` and `screen.x + screen.width -| toast_width`), hoisting the two repeated boundary sums (`screen_bottom`, `inner_right`) to compute once instead of 3-4x each. Independently re-ran `zig build test` myself (exit 0, no failures) before and after code-reviewer's pass. code-reviewer confirmed the fix eliminates the overflow with no behavior change for normal inputs (saturating ops are no-ops absent overflow), walked the full function confirming no other unguarded site remains, but flagged 2 non-blocking items: (1) 2 of the 6 new tests (testing the `inner.x+inner.width` site specifically) don't actually reach values that would have overflowed pre-fix — weaker coverage than claimed but harmless, not re-dispatched to stay scoped; (2) `Block.inner()` (`src/tui/widgets/block.zig:145,160`) has the same unguarded-`u16`-addition shape (`result.x += 1`, `+= self.padding_left`), reachable from toast_manager's `.top_left`/`.bottom_left` position when `screen.x` is near max — flagged as a follow-up lead, NOT fixed this session (would be scope creep beyond the confirmed reproduction). Committed (5fe4921) and pushed.
  - **New lead for next session**: `src/tui/widgets/block.zig:145,160`'s `Block.inner()` unguarded `u16` addition (`result.x += 1` / `+= self.padding_left`) — same bug class as this session's toast_manager fix, flagged by code-reviewer but not yet reproduced/fixed. Worth a fresh TDD cycle: first reproduce via a throwaway test with an extreme `x`/area near u16 max before committing to the fix (per session 384's "verify empirically before fixing" lesson), then check whether the same unguarded-addition shape exists in OTHER widgets' geometry helpers beyond `Block.inner()` and `toast_manager.zig` — this could be a new recurring audit well analogous to the `@intFromFloat` one (7 files, 6 sessions), just for a different arithmetic operator.
  - **Process insight**: when the two long-running tech-debt audit sources (`@panic`/`unreachable`/global-`var`/`std.debug.print` violations, and weak/tautological test assertions) are both confirmed exhaustively dry, the next-cheapest lead isn't necessarily a new widget — grepping for a *different arithmetic operator* on the same "public struct field, no validation" reachability shape that's already justified ~10 prior fixes (float casts) surfaced a genuinely new bug (integer addition) on the first file checked. Worth trying this angle (u16/usize addition/subtraction overflow, not just intFromFloat) more broadly before defaulting to "propose a new widget with zero demand signal."
  - **Current state**: latest release v2.94.2 (tagged + GitHub release). 0 open issues. 138 widget files (unchanged — no new widget this session, by design). No active milestones — next session should either continue the newly-opened "unguarded u16 addition" audit lead (starting with `Block.inner()`) or check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first per protocol.

✅ **Session 389** — FEATURE MODE (2026-07-21). CI green, 0 issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica). First closed out session 388's flagged NaN-through-`std.math.clamp` lead empirically rather than fixing it: wrote a standalone probe confirming Zig 0.15.2's `@min`/`@max` builtins are NaN-avoiding (deterministically return the non-NaN operand regardless of argument order), so `std.math.clamp(NaN, lo, hi)` (implemented as `@max(lo, @min(val, hi))`) never actually passes NaN through on this toolchain — the flagged lead was a false alarm, not a bug; closed without a code change. Then did a fresh sweep of the recurring "unclamped `@intFromFloat`" bug class (fixed 6+ times prior across bubble_chart/dot_plot/funnel_chart/waterfall_chart/profiler/error_bar_chart) across all `@intFromFloat` call sites in `src/tui/widgets/*.zig` and found a genuine new instance: `src/tui/widgets/particles.zig`'s `ParticleSystem.render()` cast `particle.x`/`particle.y` (`f32`) straight to `i32` with zero clamping — reachable two ways: (1) a caller directly constructs a `Particle` via the public `Particle.init(...)` and appends it to the public `particles: ArrayList(Particle)` field with an extreme x/y, or (2) the public `setGravity(gravity: f32)` (no validation) lets `vy` accumulate an extreme value that propagates into `y` over a couple of `update()` calls (gravity is added to `vy` *after* the position update each frame, so it takes 2 calls, not 1, for a huge `setGravity()` value to reach `y`). Manually reproduced via a throwaway test (`setGravity(1e30)` + 2×`update()` + `render()` → "integer part of floating point value out of bounds" panic at particles.zig:256), reverted the throwaway, then ran the full TDD cycle: test-writer wrote `tests/widget_particles_test.zig` (confirmed RED — panic at line 255/256) and wired it into `build.zig`; zig-developer clamped `particle.x`/`particle.y` to ±999,999,999.0 before the cast (matching the established convention) and *also* added a `px/py > 65535` guard before the `@intCast` to `u16` — a second, previously-unnoticed panic path for any position landing in `(65535, i32::max]` even without an extreme f32 value. code-reviewer confirmed the fix eliminates the panic for all four extreme classes (huge-positive, huge-negative, NaN, ±infinity — independently verified NaN/inf clamp safely too) with no regression for legitimate in-area particles, but flagged two test-quality gaps (one tautological `count()==3` assertion that didn't actually check `render()`'s behavior, and no NaN/±Infinity regression coverage) — re-dispatched test-writer to fix both before commit (16 tests total after). Independently verified `zig build test` green (0 failures) before every handoff. Committed (9fd8b42) and pushed.
  - **Note on `src/tui/particles.zig`** (the OTHER, unrelated `ParticleSystem` — confusingly, sailor has two independent particle-system implementations: `src/tui/particles.zig` exported at top-level `sailor.ParticleSystem`/`ParticleKind`/`ParticleConfig` and tested by the pre-existing `tests/particles_test.zig`, vs. `src/tui/widgets/particles.zig` exported as `sailor.tui.widgets.ParticleSystem`/`ParticleType`/`Particle` and now tested by the new `tests/widget_particles_test.zig`): audited it for the same bug shape and confirmed it's NOT vulnerable — no public gravity/velocity setter exists, all particle velocities are internally fixed to small hardcoded ranges (e.g. rain ∈ [2.0, 4.0]), and particles self-expire quickly via a time-based `lifetime_ms` countdown, so position can't accumulate to i32-overflow-triggering magnitudes even with an adversarial `delta_ms` input (worked through the arithmetic: `u32::MAX` delta_ms in one call yields ~1.7e7 displacement, well under `i32::MAX`≈2.1e9). Left unchanged — false-alarm-by-design, not a fix deferred.
  - **Naming smell noted but NOT fixed** (would be scope creep beyond the bug being chased): `src/tui/widgets/gantt_chart.zig:205-206` names two variables `x_start_unclamped`/`x_end_unclamped` even though they ARE already clamped via `@max(0.0, @min(x_f, width))` on the very same line — misleading but functionally harmless (verified NaN/±inf safe too, same `@min`/`@max` NaN-avoidance property). Low-priority rename candidate for a future cosmetic pass, not chased this session to stay scoped.
  - **Release judgment**: 4 fix commits have now accumulated unreleased since v2.94.1 (682f410 validation.zig ring buffer, 0087a9f term.zig fd=42 guard, f6f9b53 chart-widget clamps, 9fd8b42 this session's particles.zig fix) — past the "3rd consecutive fix" bundling threshold session 384 flagged as the reconsideration point (which is exactly what produced v2.94.1). 0 open bug issues. Triggered CI on the push and is waiting for the full cross-compile + native test matrix to go green before cutting a bundled patch release (v2.94.2) per protocol — see next session's entry for the outcome if this session ended before CI completed.
  - **Process insight**: the "unclamped `@intFromFloat`" audit well is still not dry — this is the 7th distinct file hit by this exact bug shape across 6 sessions (357, 372(?), 382-388, 389), and it keeps surfacing in files nobody had grepped yet (this session found it by grepping `@intFromFloat` across `src/tui/widgets/*.zig` fresh, rather than re-checking previously-fixed files). Also reinforces session 384's "verify a flagged lead empirically before either fixing or re-flagging it" lesson — the NaN-clamp lead from session 388 turned out to be a non-issue once actually tested against this Zig version, saving a wasted fix cycle.

✅ **Session 388** — FEATURE MODE (2026-07-20). CI green, 0 issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica). Found a prior session's (387, never memory-logged) uncommitted working tree: fixes across `bubble_chart.zig`/`dot_plot.zig`/`funnel_chart.zig`/`waterfall_chart.zig`/`profiler.zig` clamping `@intFromFloat` inputs to a safe ±999,999,999 range before casting to `i32` (preventing panics when a caller passes an extreme f32 "value" straight into a text-label formatter), a `PerformanceProfiler` migration to the Zig 0.15 unmanaged `ArrayList` API + `Style.fg` literal simplification (`.{ .basic = .green }` → `.green`), and a new 520-line `tests/profiler_widget_test.zig` (registered in `build.zig`) exercising extreme frame-time/sparkline edge cases — but `zig build test` still failed: `sparkline.zig`'s `getBarChar` computed `value * bar_count / (max + 1)` in `u64`, which genuinely overflows (integer-overflow panic, not just a missed clamp) when `value` is near `u64` max and `bar_count > 1`, reachable via `PerformanceProfiler.renderFrameTimes()`'s new extreme-frame-time test. Widened the scaling math to `u128` (`@as(u128, value) * @as(u128, bar_count) / max_plus_one`) rather than clamping `value` pre-multiplication, since `bar_count` is always small (bounded by the `bars` string's visible glyph count) so `u128` can hold the product exactly with no practical overflow risk. Independently re-ran `zig build test` after the fix: 11895/11895 passing, 0 failures (was 1 failure + 1 skipped-count mismatch before). Committed (f6f9b53) and pushed — this satisfies the session's "at least one meaningful commit" rule via completing prior in-progress work rather than net-new feature work.
  - **Extended audit**: since this session's fix was exactly the "raw external f32 value cast directly via `@intFromFloat` without a clamp" bug shape, swept all 44 widget files using `@intFromFloat` for any other unguarded instance of that *specific* pattern (a public-API `value: f32` field formatted straight into a label, as opposed to a geometry/position calculation derived from a bounded ratio × `u16` area dimension, or a trig `sin`/`cos` output × bounded radius — both of which are inherently bounded and were not in scope). Grepped for the exact `@intFromFloat(value)`/`@intFromFloat(self.value)`/`@intFromFloat(item.value)`/etc. idiom post-fix: zero remaining hits — the label-overflow bug class appears now fully patched across the widget catalog (all 6 instances were the ones already fixed this session + session 387: bubble_chart, dot_plot, funnel_chart ×2, waterfall_chart, profiler).
  - **Noted but explicitly NOT pursued this session** (would be new speculative work, not completing existing work): `std.math.clamp(value, lo, hi)` does not sanitize `NaN` (NaN fails both `<`/`>` comparisons so passes through unclamped), meaning any widget that clamps a `ratio`/`value` field via `std.math.clamp` before an `@intFromFloat` cast (e.g. `gauge.zig`, `reactive.zig`, `splitpane.zig` — all clamp-then-cast) could still panic if a caller passes `std.math.nan(f32)` directly into a public struct field (bypassing the clamping builder method, since these are plain public-field structs). This is a real but *speculative* robustness gap (no reported bug, no reproduction from consumer usage) spanning many files — flagged as a candidate tech-debt source for a future session rather than chased here, to avoid scope creep beyond "complete session 387's already-in-progress fix."
  - **Current state**: latest release still v2.94.1 (unchanged — this was a proactive bug fix completing prior uncommitted work, not a consumer report/CI break/cross-compile break, so per sessions 382-386's precedent does not auto-trigger a patch release on its own). 0 open sailor issues. No active milestones (10th consecutive session) — next session should check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first; if still empty, either pursue the NaN-through-`std.math.clamp` lead flagged above, or proactively draft a milestone per the Milestone Establishment Process.

✅ **Session 386** — FEATURE MODE (2026-07-20). CI green, 0 issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica). Continued the rule-violation grep audit (`@panic`/`unreachable`/`std.debug.print`/global `var` outside tests) — `stack_trace.zig`'s `@panic` (documented opt-in assert utility, session 384) and `debug_log.zig`'s global `var`s (documented opt-in env-based debug logger, only referenced from `sailor.zig`, no internal callers — same "intentional opt-in utility" shape as `stack_trace.zig`) are both confirmed non-issues, not re-flagged. Found a genuine new bug in `src/term.zig`: `queryTerminalCapability()`'s Unix branch routed any fd exactly equal to `42` into the test-only mock path unconditionally, without checking whether `global_mock_terminal` was actually installed (the Windows branch already had this guard — `if (global_mock_terminal) |mock| { if (@intFromPtr(fd) == @intFromPtr(mock)) ... }` — but the Unix branch was just `if (fd == 42)`). Since `global_mock_terminal` is always `null` in real (non-test) usage, a genuine caller-supplied fd numbered exactly 42 (plausible in a long-running process with many open fds) would silently get misrouted into the dead mock branch and receive `error.QueryTimeout` instead of a real xtgettcap capability query attempt. Ran TDD cycle: `test-writer` added a regression test calling `queryTerminalCapability(allocator, 42, ...)` with no mock installed, asserting `error.NotATty` (matching the behavior of any other invalid fd like the existing `9999` tests) — confirmed red (`expected error.NotATty, found error.QueryTimeout`). Fixed with a one-line guard: `if (fd == 42 and global_mock_terminal != null)`, mirroring the Windows branch's existing pattern; also trimmed the test-writer's overly narrative bug-hunting comment block down to a concise regression-test comment since "expected result: FAIL (red)" language goes stale the moment the fix lands. `code-reviewer` confirmed safe to commit (0 critical/warning, 1 non-blocking suggestion noting `global_mock_terminal` itself is pre-existing test-only global state, not newly introduced) — independently verified all 8 existing mock-routing tests still pass (they set `global_mock_terminal` before calling, so both conjuncts still hold) and no other unguarded sentinel/global-routing pattern exists elsewhere in the file. One unrelated pre-existing flaky test (`validation.test.async validator - non-blocking`, a timing-sensitive async test) failed once then passed on rerun with no code changes — confirmed not caused by this fix. Committed (0087a9f) + pushed.
  - **Release judgment**: proactive internal-audit fix (not a consumer bug report, not a CI failure, not a cross-compile break) — per the narrower patch-release trigger conditions and sessions 382-385's precedent, this does NOT auto-trigger a release on its own; it's fix #1 since the last release (v2.94.1), so deferred pending a 3rd-consecutive-fix threshold or an actual reported bug.
  - **Process insight**: the `@panic`/`unreachable`/global-`var` grep audit well is not fully dry — it just needed a different lens this round (magic-sentinel-value routing without an existence guard, rather than the `@panic`-on-overflow or ring-buffer-wraparound shapes fixed in prior sessions). When auditing mock/test-seam code embedded in production functions, specifically check whether the sentinel/routing condition validates that the "test mode" state is actually active, not just that a magic value matches — a magic value alone can collide with legitimate production input.
  - **Current state**: latest release still v2.94.1 (unchanged this session). 0 open sailor issues. No active milestones (8th consecutive session) — next session should check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first; if still empty, either continue the rule-violation audit (remaining unswept files: haven't yet grepped `examples/` or `tests/` helper files for the same magic-sentinel-without-guard pattern) or proactively draft a milestone.

✅ **Session 385** — STABILIZATION (2026-07-19). CI green, 0 issues at session start. Resolved session 384's flagged lead: `CombinedValidator` (`src/validation.zig`, was ~lines 90-146) held module-global mutable ring-buffer state (`var storage: [32]Entry`, `var next_idx: usize`) with two real bugs — (1) after 32 `combine()` calls, older validators silently got remapped to a different combination (ring-buffer wraparound), and (2) it stored the caller's validator slice by reference with no ownership/copy (use-after-free risk on stack-allocated caller slices). Since the root cause was architectural (`ValidateFn` was a bare `*const fn([]const u8) ValidatorResult` with no context slot — the only reason the old code needed a global comptime-trampoline table at all), ran the full team protocol: `architect` first (decided: give `ValidateFn`/`Validator` a `ctx: ?*const anyopaque` field + `validate()` wrapper method, and change `combine()` to take a caller-owned `CombinedStorage` struct pointer instead of writing into global storage — rejected a heap-allocator variant since `Validator` is used everywhere as a trivial value type with no `deinit` convention, and rejected a `comptime validators` variant since composed validator lists are plausibly assembled at runtime), then `test-writer` (migrated all ~59 `validator.validateFn(x)` call sites in `tests/validation_test.zig` to `validator.validate(x)`, added `var storage: CombinedStorage = undefined;` + `&storage` to all ~11 existing `combine()` call sites, added 3 new regression tests proving the ring-buffer/cross-contamination bugs are actually fixed — confirmed RED via 59 compile errors, the correct red state for an API-shape refactor), then `zig-developer` (implemented the ctx-pointer API, deleted `CombinedValidator` entirely, rewrote `combine()` around caller-owned `CombinedStorage` — correctly stopped and reported rather than fixing a test-writer test bug itself: one new regression test tried `Validator.minLength(min_val)` with a runtime loop variable, which doesn't compile since `minLength` takes `comptime min: usize` per session 384's fix). Re-dispatched `test-writer` to fix its own test (`inline for (0..39) |i|` makes `i` comptime-known, satisfying `minLength`), then `zig build test` passed fully. `code-reviewer` confirmed clean (0 critical/warning, 2 doc-comment suggestions — added a note that `combine()`'s `validators` slice, like `storage`, must outlive the returned `Validator`'s use). Independently verified: no module-level `var` remains in `src/validation.zig`, no `@panic`, all `.validateFn(` call sites across `src/`/`tests/`/`examples/` migrated. Committed (682f410) + pushed.
  - **Release judgment**: this is a proactive internal-audit fix (not a consumer bug report, not a CI failure, not a cross-compile break), so per the narrower patch-release trigger conditions (and sessions 382/383's precedent) it does NOT auto-trigger a release — only the 3rd-consecutive-fix threshold (hit last session, resulting in v2.94.1) forces a bundled release; this is fix #1 since that release, so deferred. Ran the stabilization-session cross-compile check (all 6 targets: x86_64/aarch64 × linux/macos/windows, sequential, no concurrent zig build process) — all green.
  - **Process insight**: when a flagged bug's root cause is a type-system constraint (bare function pointer can't carry closure context), the fix is inherently an architecture-level API change, not a local patch — worth explicitly routing through `architect` first (per CLAUDE.md's team-formation rule) rather than having zig-developer freelance a design. The TDD agent-boundary held again: zig-developer correctly refused to fix test-writer's own comptime-argument bug in a new test, re-dispatching back to test-writer instead of quietly patching it itself.
  - **Current state**: latest release v2.94.1 (unchanged this session — no release-worthy trigger). 0 open sailor issues. No active milestones (7th consecutive session) — next session should check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first; if still empty, either continue tech-debt audits (this `@panic`/`unreachable`/global-`var` grep source may finally be dry — worth a fresh full-repo grep to confirm) or proactively draft a milestone.

✅ **Session 384** — FEATURE MODE (2026-07-19). CI green, 0 issues. Resolved session 383's two flagged leads: `src/stack_trace.zig:30`'s `@panic` is confirmed **intentional, not a violation** — the whole module (`panicWithContext`/`assert`/`require`/`ensure`/`unreachable_`) is a documented opt-in assert/debug utility analogous to `std.debug.assert`, exported as public API (`sailor.stack_trace`) for consumers who explicitly choose to call it; not used internally by any other src/ code, so it's not a silent landmine reachable through normal operation. `src/validation.zig:54-55`'s static buffers WAS a real bug: `Validator.minLength()`/`maxLength()` dispatched via a runtime `switch` over a hardcoded preset ({5,10,20} min, {10,100} max) and hit `else => unreachable` for any other value — a genuine crash/UB reachable from the public API by any caller wanting a non-preset length (this library has zero internal callers of these functions — pure public-API surface, e.g. for form validation) — plus the error messages were formatted into module-global mutable `var` buffers shared across ALL validators (a "no global state" violation + data race). Ran full TDD cycle: test-writer added 9 tests to `tests/validation_test.zig` (confirmed red — `zig build test` crashed with "reached unreachable code" at the old `minLength` switch, calling `minLength(3)`); zig-developer changed `minLength`/`maxLength` to take `comptime min/max: usize` and build the message via `std.fmt.comptimePrint` stored as a `const msg` inside the generated closure — this eliminates the `unreachable` branch entirely (any comptime value now works, no preset needed) and removes the global buffers (message is a compile-time constant per instantiation, no runtime formatting/storage needed). Removed now-dead presets (`min_5_fn`/`min_10_fn`/`min_20_fn`/`max_10_fn`/`max_100_fn`). code-reviewer confirmed safe to ship (0 critical/warning findings); independently verified `zig build`/`zig build test` clean, `grep` shows the buffers/presets/unreachable branch fully gone (only the genuinely-safe `CombinedValidator` idx-modulo-32 `unreachable` remains, correctly left alone). Committed (9e11b4e) and pushed.
  - **Release judgment**: this was the 3rd consecutive proactive rule-violation fix accumulating unreleased since v2.94.0 (a3a0165 LineBuilder session 382, 2cf6c8f CountdownTimer session 383, 9e11b4e validation.zig this session) — session 383 explicitly flagged this exact threshold ("if a 3rd such fix accumulates, reconsider a bundled patch release"). Waited for CI on the new commit (all 6 cross-compile targets + native test matrix green), then cut a bundled patch: tagged/released **v2.94.1**, filed consumer migration issues (zr#143, zoltraak#109, silica#121, noting no breaking changes), Discord notified.
  - **New lead for next session**: code-reviewer flagged `CombinedValidator` (`src/validation.zig` ~lines 96-161, used by `Validator.combine()`) as the same architectural shape as the bug just fixed — a global mutable `storage: [32]Entry`/`next_idx` ring buffer. Two distinct issues there, NOT yet fixed: (1) after 32 `combine()` calls, older validators silently get remapped to a *different* combination (ring buffer overwrite — a stale-behavior correctness bug, not just thread-safety), and (2) `storage[idx].validators = validators` stores the caller-supplied slice **by reference** with no ownership/copy — if the caller's `validators: []const Validator` array is stack-allocated and goes out of scope before the returned `Validator` is used, this is a use-after-free. Worth a dedicated TDD cycle: likely needs `Validator`/`ValidateFn` to carry a context pointer (breaking API shape change) rather than a bare function pointer, since the current design has no way to give each `combine()` result its own owned storage. Also noted but out of scope: `src/validation.zig:73` grammar nit ("1 characters" should be "1 character" for count=1) — cosmetic, not a correctness bug.
  - **Process insight**: not every flagged `@panic`/`unreachable` lead is a bug — `stack_trace.zig` was a false positive (intentional assert utility), `validation.zig` was a true positive (accidental landmine). The distinguishing test: is the panic/unreachable the *documented purpose* of the function (like `std.debug.assert`), or does it fire on inputs a normal caller would reasonably pass to an otherwise-innocuous-sounding function (like `minLength(7)`)? Read the doc comments and check for internal-vs-external-only usage before triaging.

✅ **Session 383** — FEATURE MODE (2026-07-19). CI green, 0 issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica). Continued session 382's "grep for hard-rule violations in non-test code" tech-debt source (widget/PRD/weak-assertion wells confirmed dry again) and found a second, independent `@panic` violation: `src/tui/widgets/countdown_timer.zig`'s `formatMmSs`/`formatHhMmSs`/`formatSeconds` called `@panic(...)` when `std.fmt.bufPrint` overflowed their fixed 9-byte buffer — reachable from the public `render()` path with realistic inputs (e.g. any `.hh_mm_ss` countdown beyond ~41 days, i.e. `total_seconds >= 3,600,000`, already overflowed). Ran full TDD cycle: test-writer added 12 tests to `tests/countdown_timer_test.zig` (4 that reproduced the actual panic, 8 asserting exact clamped output strings); zig-developer clamped `hours`/`mins`/`seconds` to the max value provably fitting the buffer (999/999,999/999,999,999 respectively) before formatting, then changed `catch @panic(...)` to `catch unreachable` (now provably safe). **TDD-boundary in action**: zig-developer correctly did NOT fix 4 self-inflicted arithmetic errors in test-writer's own new tests (an impossible `len==8` for a 9-char string; a boundary input off by 10x — `5_999_999` instead of `59_999_999`) — re-dispatched test-writer to fix its own tests per the "테스트 수정은 test-writer가" rule; final `zig build test` 119/119 pass. Independently verified: `grep -n "@panic" src/tui/widgets/countdown_timer.zig` empty, `zig build`/`zig build test` both clean. Committed (2cf6c8f) and pushed.
  - **Release judgment**: two `fix:` commits now sit unreleased since v2.94.0 (a3a0165 LineBuilder + 2cf6c8f CountdownTimer), and no open bug issues / no CI break / no cross-compile break exist to trigger the specific patch-release conditions (CLAUDE.md's narrower "트리거 조건" bullet list, not just the top-level "fix: 커밋만 존재 → PATCH" summary table). Followed session 382's precedent and did NOT release — consumers only pull tagged releases (`zig fetch` against a tag), so unreleased fixes on `main` don't reach them until a tag is cut, and releasing on every proactive internal-audit fix (vs. an actual reported/CI-caught failure) risks patch-version churn with zero demand signal. Flagged for the next session: if a 3rd such proactive panic/rule-violation fix accumulates unreleased, reconsider cutting a bundled patch release rather than deferring indefinitely.
  - **Process insight**: the same rule-violation grep source (`@panic`/`unreachable`/`std.debug.print`/global `var` in non-test code) that surfaced session 382's bug immediately surfaced a second, independent one in a different file — this tech-debt well is not yet dry; also flagged but NOT fixed this session (lower confidence, likely intentional): `src/stack_trace.zig:30` `@panic(msg)` inside what looks like a debug-only assert helper (needs source read to confirm intentional vs. violation), and `src/validation.zig:54-55`'s module-level `var min_length_error_msg`/`max_length_error_msg: [256]u8` static buffers (potential thread-safety/global-state issue if `no global state` applies to non-widget helper modules — needs investigation, not yet triaged).
  - **Current state**: latest release still v2.94.0 (no release this cycle). 0 open sailor issues. No active milestones (6th consecutive session) — next session should either continue the `@panic`/`unreachable`/global-`var` audit (concrete unresolved leads: `stack_trace.zig:30`, `validation.zig:54-55` static buffers) or, if that's confirmed clean/intentional too, proactively draft a milestone per the Milestone Establishment Process rather than waiting indefinitely on `gh issue list`.

✅ **Session 382** — FEATURE MODE (2026-07-18). CI green, 0 issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica — 5th consecutive session with nothing to replenish a milestone from; zr has a growing backlog of unaddressed `migration` issues #133-142, but per protocol code migration is zr's own agent's job, not sailor's). Weak-assertion audit series (sessions 354-381) is now exhaustively clean per session 381, so this session searched for a different class of tech debt: grepped `src/` for `@panic`/`unreachable` outside test blocks and found a genuine CLAUDE.md rule violation — `src/tui/style.zig`'s `LineBuilder.span()/raw()/text()` called `@panic("... allocation failed")` on allocator OOM instead of returning an error, violating "No @panic in library code". Ran full TDD cycle: test-writer added 3 `FailingAllocator`-based tests to `tests/span_builder_test.zig` proving the gap (confirmed red — compile error `expected error union type, found '*tui.style.LineBuilder'`, not just a failing assertion); zig-developer changed the 3 signatures to `Allocator.Error!*LineBuilder` and replaced `catch @panic(...)` with `try`, then fixed all ~23 call sites across `src/tui/line_break.zig`/`src/tui/richtext_parser.zig`/`tests/span_builder_test.zig`/`tests/richtext_parser_test.zig` (mechanical `_ = try builder.X(...)` insertion — no call site anywhere in the codebase fluently chained these builder methods, so no API redesign was needed beyond the signature change). Independently verified: `grep -n "@panic" src/tui/style.zig` empty, `grep` for un-`try`'d call sites empty, `zig build` and `zig build test` both clean. Committed (a3a0165) and pushed. Not a release-worthy change (no consumer bug report, no CI break, no cross-compile break — patch-release trigger conditions per CLAUDE.md aren't met), so no version bump this cycle.
  - **Process insight**: when the recurring "audit weak assertions" well runs dry, the next-cheapest tech-debt source is grepping for the library's own hard rules being violated in non-test code (`@panic`, `unreachable`, `std.debug.print`, global `var`) rather than inventing another chart widget with zero demand signal — this project already has 138 widgets, full theming (`theme.zig`/`theme_editor.zig`), animation (`animation.zig`/`animated_text.zig`/`animated_border.zig`/`transition.zig`), and screen-reader support (`screen_reader.zig`), so PRD Phase 6 "polish" items are not a gap either.
  - **Current state**: latest release still v2.94.0 (no release this cycle). 0 open sailor issues. No active milestones (5th consecutive session) — next session should check whether the weak-assertion / rule-violation tech-debt wells are truly dry before defaulting to another widget; if so, proactively draft a milestone (new widget or TUI-core feature) rather than waiting indefinitely on `gh issue list`.

✅ **Session 381** — FEATURE MODE (2026-07-18). CI green, 0 issues, 0 feature-requests anywhere (sailor/zr/zoltraak/silica — 4th consecutive session with nothing to replenish a milestone from). With no PRD gaps/issues/feature-requests available, used milestone-establishment input source 4 (기술 부채) and continued the recurring weak-assertion audit: found 38 more tautological `expect(x >= 0)` assertions (usize/u64 types that can never be negative) across 10 files (`violin_plot`, `candlestick_chart`, `parallel_coordinates`, `radar_chart`, `ridgeline_plot`, `gantt`, `keymap`, `advanced_profiler`, `platform_opts`, `calendar` test suites), plus one same-outcome-either-way disjunction in `calendar_test.zig` (`selected == null or selected.?.compare(...) >= 0`) that no longer matched real `selectDate()` behavior (it's a no-op on rejection, so `selected` deterministically stays `null` — not "either null or clamped"). Delegated to `test-writer` per TDD-agent-boundary rule.
  - **First pass surfaced 4 apparent regressions** (`non_empty == 0` expected, `> 0` actual) in degenerate-OHLC candlestick tests and a tiny-fractional-value violin plot test — investigated by reading the render source myself before accepting them as bugs: `candlestick_chart.zig`'s `valueToRow.calc` explicitly centers a doji marker when `max_val == min_val` (intentional degenerate-case design, confirmed at lines 216-218), and `violin_plot` normalizes by range regardless of absolute magnitude (same pattern as the adjacent "very large values" test, which correctly asserted `> 0`). Both were **wrong test-writer guesses, not implementation bugs** — re-dispatched test-writer with the source-code reasoning to flip these 4 to `> 0`. Final `zig build test`: 0 failures.
  - **Process insight**: when a strengthened assertion fails, don't hand it straight to zig-developer as a "confirmed bug" — read the actual render/logic source first. A failing test after an assertion-strengthening pass can mean either (a) a real shipped bug, as in sessions 375/354/357/380, or (b) the auditor guessed the wrong expected outcome for a legitimate degenerate-case design (this session). Verify against source before deciding which.
  - Audit for this pattern (`expect(x >= 0)` on unsigned types, `expect(true)` placeholders) now appears **exhaustively clean** across `tests/*.zig` — a targeted regrep after this session's fixes found zero remaining instances except the legitimate `i16`-sentinel "was it found" checks in `activity_feed_test.zig`/`gantt_test.zig` (verified as real, not tautological, left untouched).
  - **Current state**: latest release still v2.94.0 (no release this cycle — test-only changes, no version bump warranted). 0 open issues. No active milestones (4th consecutive session) — next session should seriously consider proactively drafting a milestone from PRD/consumer-gap analysis rather than waiting indefinitely on `gh issue list`, since the weak-assertion audit well may be running dry.

✅ **Session 380** — STABILIZATION (2026-07-18). CI green, 0 issues at session start. Found a prior session's uncommitted **DonutChart** widget (312-line widget + 1013-line/56-test suite already wired into `src/sailor.zig`/`src/tui/tui.zig`) — but unlike prior "uncommitted-but-green" finds, `build.zig` was missing the `donut_chart_tests` registration entirely, so the suite had *never actually run*; `zig build test` being green proved nothing about it. Registering it surfaced a compile error first (an `orelse .{ anon struct literal }` type-inference failure inside a boolean `and` expression — fixed by switching to `if (x) |cell| ... else ...` instead of relying on peer-type resolution through the field-access chain), then a real rendering bug the suite's own regression test predicted: `render()` centered `center_label` on the pre-legend-split `inner` area instead of the post-split `chart_area` used by `renderDonut()`, misaligning the label whenever the default `legend_position=.right` was active. Fixed by passing `chart_area`; corrected 4 other tests that had encoded the old buggy-center pixel coordinates as literals.
  - **Also discovered**: `docs/milestones.md` claimed v2.94.0 (ErrorBarChart) was already released, but `build.zig.zon` still read 2.93.0 and no `v2.94.0` git tag/GitHub release existed — the exact "checklist says `[x]` but the release never happened" gap caught before in session 354. Bundled ErrorBarChart + the newly-completed DonutChart into one real v2.94.0 release: bumped `build.zig.zon`, verified all 6 cross-compile targets sequentially (stabilization session, allowed), tagged, pushed, created the GitHub release, filed consumer migration issues (zr#142, zoltraak#108, silica#120), Discord notified.
  - Continued the session 354/360/370/375 weak-assertion audit: found 20 tautological `expect(non_empty >= 0)` / `expect(countNonEmptyCells(...) >= 0)` assertions (always true on `usize`) across `box_plot_test.zig`, `bullet_chart_test.zig`, `calendar_heatmap_test.zig`, plus 3 `expect(true)` placeholders in the latter. Delegated the fix to `test-writer` (per TDD-agent-boundary rule — test-only changes still route through test-writer, not the orchestrator directly). Strengthened all 26 to real checks (specific glyph presence/absence via the existing `countChar` helper, exact-zero assertions where the widget's documented behavior implies no output). **No new bugs surfaced this pass** — first weak-assertion sweep in this recurring series that didn't uncover a real implementation bug, suggesting the widget code in these three files was already correct going in.
  - **Process insight**: "uncommitted, and `zig build test` exits 0" is not sufficient verification when a *new* test file might not be wired into `build.zig` at all — always grep `build.zig` for the new test's registration before trusting a green exit code as proof the suite ran. This is a distinct failure mode from the "tests exist and ran but assertions are weak" pattern documented in sessions 354/360/370/375.
  - **Current state**: latest release v2.94.0 (tagged + GitHub release, matches build.zig.zon), 0 open issues, 138 widget files. No active milestones — needs replenishment next session (check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first, per Milestone Establishment Process).

✅ **Session 377** — FEATURE MODE (2026-07-17). CI green, 0 issues at session start. Found a prior session's fully-implemented-and-passing-but-uncommitted **CalendarHeatmap** widget (467-line widget + 898-line/54-test suite, already wired into `build.zig`/`src/sailor.zig`/`src/tui/tui.zig`, plus a `Calendar.setRange()` helper) — the `.claude/logs/agent-activity.jsonl` diff showed the full TDD cycle (test-writer → zig-developer → code-reviewer) had already run at 2026-07-17T00:28–00:52Z but was never committed. Independently verified via `zig build test` (0 failures) and `zig build` before trusting it, per the recurring session pattern (372/374/376-ish) of "uncommitted-but-green work from a prior cycle." Committed, ran all 6 cross-compile targets sequentially (all green), released **v2.93.0**. Filed consumer migration issues: zr#141, zoltraak#107, silica#119. No feature-request issues open anywhere (sailor/zr/zoltraak/silica) — milestone list is empty again, carried to next session same as 374/375.
  - **Current state**: latest release v2.93.0, 0 open issues, 137 widget files. No active milestones — needs replenishment (2nd consecutive session with nothing to replenish from; may need to proactively propose a widget from PRD gaps rather than waiting on issues).

✅ **Session 375** — STABILIZATION (2026-07-17). CI green, 0 issues at session start. Continued the session 360/370 weak-disjunction-assertion audit and found 5 files the earlier sweep had missed (`bubble_chart`, `flowchart`, `gantt`, `gantt_chart`, `matrix_view` test suites — 15 instances of `<claim> or countNonEmptyCells(...) > N`). Strengthening these to strict assertions uncovered a **real bug**: FlowChart rendered edges (arrows/labels) before nodes, so node borders overwrote them — fixed render order (nodes first, then edges on top). Released **v2.92.1** (patch).
  - Then found 6 meaningless `expect(true)` placeholder assertions in `toggle_switch_test.zig` (added uncommitted/unverified in session 374's ToggleSwitch find — codebase convention elsewhere always pairs "does not panic" with a real state check, toggle_switch was the outlier). Fixing the "applies base style to items" placeholder uncovered a **second real bug**: `ToggleSwitchGroup.render()` never applied `self.style` (group base style) to items at all — silently dropped since v2.92.0. Fixed with `applyGroupStyle`/`mergeStyles` helpers (group style as base, item's own sub-styles override). Released **v2.92.2** (patch, supersedes v2.92.1).
  - **Process insight**: both bugs were shipped in v2.92.0 and only surfaced because a test-quality audit forced weak assertions into strict ones — reinforces that "meaningful tests only" (CLAUDE.md rule 15) isn't cosmetic, it's the actual bug-detection mechanism. When a newly-added widget/file is found "uncommitted but green" (session 374 pattern), its tests deserve the same weak-assertion scrutiny as older files, not a pass because `zig build test` was green.
  - Full 6-target cross-compile verified for both patch releases. Consumer migration issues filed for v2.92.1 (zr#140, zoltraak#106, silica#118) then updated in-place with a comment pointing to v2.92.2 rather than filing 3 more duplicate issues.
  - **Current state**: latest release v2.92.2, 0 open issues, 135 widget files. No active milestones (still needs replenishment — carried over from session 374, not addressed this cycle since stabilization work took priority).

✅ **Session 374** — FEATURE MODE (2026-07-16)
  - **Mode**: NORMAL (session 374, 374 % 5 == 4)
  - **Achievement**: Found a prior session's fully-implemented-and-passing-but-uncommitted ToggleSwitch widget, verified independently, committed, and released v2.92.0.

  **Completed Work**:
    - ✅ Found uncommitted working tree at session start: `src/tui/widgets/toggle_switch.zig` (360 lines) + `tests/toggle_switch_test.zig` (66 tests) already wired into `build.zig`/`src/sailor.zig`/`src/tui/tui.zig`. Unlike session 372's IcicleChart find, this one was fully green (`zig build test` exit 0, no `@panic`/stdout/global state) — closer to sessions 363/369's "just never committed" pattern than 372's "looked done but wasn't."
    - ✅ Confirmed CI green (latest 3 runs success/cancelled) and 0 open bug issues before treating this as ready. Note: ToggleSwitch was not on the prior future-candidate list (which was empty after v2.91.0/IcicleChart) — it's a form-control widget (slider-style boolean toggle, distinct from Checkbox's tick-mark convention) rather than the recent run of dataviz/chart widgets, but fully justified as its own widget with no overlap.
    - ✅ Committed the widget (b83360f). Ran all 6 cross-compile targets sequentially (linux/macos/windows × x86_64/aarch64, ReleaseSafe) after confirming no concurrent `zig build` process — all exit 0.
    - ✅ Released v2.92.0: bumped build.zig.zon, added milestone entry to docs/milestones.md (retroactive — widget was already built, so documented after the fact rather than before), tagged, pushed, GitHub release created.
    - ✅ Consumer migration issues filed: zr#139, zoltraak#105, silica#117.

  **Current State**:
    - **Latest release**: v2.92.0 (tagged + GitHub release)
    - **Open issues**: 0 (sailor)
    - **Widget count**: 136 widgets in src/tui/widgets/ (toggle_switch added)
    - **Active milestones**: 0 — needs replenishment next session (future-candidate list empty; check `gh issue list --label feature-request` across sailor/zr/zoltraak/silica first)

  **Next Priority**:
    - Establish next milestone from feature-request issues, PRD gaps, or consumer feedback — no widget currently queued.

✅ **Session 372** — FEATURE MODE (2026-07-16). Found a prior session's uncommitted IcicleChart widget (v2.91.0) with 4 genuinely failing tests — root-caused the focused-path bug (was comparing path length vs depth instead of validating the actual index chain) and a second unshipped bug (`show_values` had a hardcoded TODO placeholder behind weak `countNonEmptyCells > 0` tests). **Process insight**: `zig build test` exit code is the only trustworthy "done" signal — never infer completeness from file size or doc-comment polish; when fixing one bug, grep the same file for other TODO/placeholder markers. Released v2.91.0.

✅ **Session 370** — STABILIZATION (2026-07-15). CI green, 0 issues — continued session 360's weak-disjunction-assertion audit (`or countNonEmptyCells(...) > N`) across mindmap/radar_chart/bracket_viewer/wordcloud, fixed 9 instances (bracket_viewer audited clean). Full 6-target cross-compile verified. No release (test-only changes).

✅ **Sessions 363–369** (compressed) — Released v2.86.0–v2.91.0: ParetoChart, SlopeChart, RidgelinePlot, BumpChart, MosaicPlot, IcicleChart. Recurring pattern: several of these were found as prior-session fully-implemented-but-uncommitted work (363/369) — always independently verify via `zig build test` + grep the claimed wiring edits (`src/sailor.zig`/`src/tui/tui.zig`/`build.zig`) before trusting an agent's self-report of test count or wiring completeness (gaps found in sessions 361/364). Session 364 fixed a red Windows CI flake (exact-boundary timing assertion in `profiler.zig` needed ~10% tolerance) before feature work, per protocol (CI-red overrides mode).

✅ **Session 361** — Released v2.84.0 (BulletChart). Process insight: agent claimed `sailor.zig` exports were wired but grep showed they were missing — always verify independently.

✅ **Session 360** — STABILIZATION: found the `countNonEmptyCells(...) > 0` disjunction anti-pattern (masks real bugs, e.g. session 354's BoxPlot) in 26 test files; fixed flowchart this cycle, remainder completed session 370. Released patch v2.83.1.

✅ **Session 357** — Released v2.83.0 (CandlestickChart). Fixed a shipped panic: unclamped OHLC values overflowed `@intCast` on row mapping — clamp both the normalized ratio AND the final row index, not just the lower bound.

✅ **Session 354** — Completed a stalled v2.81.0 release (checklist was `[x]` but version/tag never bumped — a checked box isn't proof, verify against `build.zig.zon` + `git tag -l`) + released v2.82.0 (BoxPlot). Caught a real bug pre-commit: box-drawing `row_q1 <= row_q3` comparison was backwards, masked by weak `countNonEmptyCells > 0` tests.

✅ **Session 352** — Released v2.80.0 (ViolinPlot: mirrored density silhouette, global min/max binning).

✅ **Sessions 346–351** (compressed) — Released v2.75.0–v2.79.0: WaterfallChart, FunnelChart, DotPlot, RadialBar, StreamGraph. Recurring gotchas: `@floatFromInt` in struct literals needs explicit `@as(f32, ...)` in Zig 0.15.x; symmetry assertions must exclude the label column; pin macOS ARM64 CI runner to macos-15 (macos-latest→26 broke Zig 0.15.2 linking).
